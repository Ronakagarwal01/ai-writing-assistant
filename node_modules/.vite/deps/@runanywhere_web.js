import {
  __publicField
} from "./chunk-JVWSFFO4.js";

// node_modules/@runanywhere/web/dist/types/enums.js
var SDKEnvironment;
(function(SDKEnvironment2) {
  SDKEnvironment2["Development"] = "development";
  SDKEnvironment2["Staging"] = "staging";
  SDKEnvironment2["Production"] = "production";
})(SDKEnvironment || (SDKEnvironment = {}));
var ExecutionTarget;
(function(ExecutionTarget2) {
  ExecutionTarget2["OnDevice"] = "onDevice";
  ExecutionTarget2["Cloud"] = "cloud";
  ExecutionTarget2["Hybrid"] = "hybrid";
})(ExecutionTarget || (ExecutionTarget = {}));
var LLMFramework;
(function(LLMFramework2) {
  LLMFramework2["CoreML"] = "CoreML";
  LLMFramework2["TensorFlowLite"] = "TFLite";
  LLMFramework2["MLX"] = "MLX";
  LLMFramework2["SwiftTransformers"] = "SwiftTransformers";
  LLMFramework2["ONNX"] = "ONNX";
  LLMFramework2["ExecuTorch"] = "ExecuTorch";
  LLMFramework2["LlamaCpp"] = "LlamaCpp";
  LLMFramework2["FoundationModels"] = "FoundationModels";
  LLMFramework2["PicoLLM"] = "PicoLLM";
  LLMFramework2["MLC"] = "MLC";
  LLMFramework2["MediaPipe"] = "MediaPipe";
  LLMFramework2["WhisperKit"] = "WhisperKit";
  LLMFramework2["OpenAIWhisper"] = "OpenAIWhisper";
  LLMFramework2["SystemTTS"] = "SystemTTS";
  LLMFramework2["PiperTTS"] = "PiperTTS";
})(LLMFramework || (LLMFramework = {}));
var ModelCategory;
(function(ModelCategory2) {
  ModelCategory2["Language"] = "language";
  ModelCategory2["SpeechRecognition"] = "speech-recognition";
  ModelCategory2["SpeechSynthesis"] = "speech-synthesis";
  ModelCategory2["Vision"] = "vision";
  ModelCategory2["ImageGeneration"] = "image-generation";
  ModelCategory2["Multimodal"] = "multimodal";
  ModelCategory2["Audio"] = "audio";
})(ModelCategory || (ModelCategory = {}));
var ModelFormat;
(function(ModelFormat2) {
  ModelFormat2["GGUF"] = "gguf";
  ModelFormat2["GGML"] = "ggml";
  ModelFormat2["ONNX"] = "onnx";
  ModelFormat2["MLModel"] = "mlmodel";
  ModelFormat2["MLPackage"] = "mlpackage";
  ModelFormat2["TFLite"] = "tflite";
  ModelFormat2["SafeTensors"] = "safetensors";
  ModelFormat2["Bin"] = "bin";
  ModelFormat2["Zip"] = "zip";
  ModelFormat2["Folder"] = "folder";
  ModelFormat2["Proprietary"] = "proprietary";
  ModelFormat2["Unknown"] = "unknown";
})(ModelFormat || (ModelFormat = {}));
var FrameworkModality;
(function(FrameworkModality2) {
  FrameworkModality2["TextToText"] = "textToText";
  FrameworkModality2["VoiceToText"] = "voiceToText";
  FrameworkModality2["TextToVoice"] = "textToVoice";
  FrameworkModality2["ImageToText"] = "imageToText";
  FrameworkModality2["TextToImage"] = "textToImage";
  FrameworkModality2["Multimodal"] = "multimodal";
})(FrameworkModality || (FrameworkModality = {}));
var ComponentState;
(function(ComponentState2) {
  ComponentState2["NotInitialized"] = "notInitialized";
  ComponentState2["Initializing"] = "initializing";
  ComponentState2["Ready"] = "ready";
  ComponentState2["Error"] = "error";
  ComponentState2["CleaningUp"] = "cleaningUp";
})(ComponentState || (ComponentState = {}));
var SDKComponent;
(function(SDKComponent2) {
  SDKComponent2["LLM"] = "llm";
  SDKComponent2["STT"] = "stt";
  SDKComponent2["TTS"] = "tts";
  SDKComponent2["VAD"] = "vad";
  SDKComponent2["VLM"] = "vlm";
  SDKComponent2["Embedding"] = "embedding";
  SDKComponent2["Diffusion"] = "diffusion";
  SDKComponent2["SpeakerDiarization"] = "speakerDiarization";
  SDKComponent2["VoiceAgent"] = "voice";
})(SDKComponent || (SDKComponent = {}));
var RoutingPolicy;
(function(RoutingPolicy2) {
  RoutingPolicy2["OnDevicePreferred"] = "onDevicePreferred";
  RoutingPolicy2["CloudPreferred"] = "cloudPreferred";
  RoutingPolicy2["OnDeviceOnly"] = "onDeviceOnly";
  RoutingPolicy2["CloudOnly"] = "cloudOnly";
  RoutingPolicy2["Hybrid"] = "hybrid";
  RoutingPolicy2["CostOptimized"] = "costOptimized";
  RoutingPolicy2["LatencyOptimized"] = "latencyOptimized";
  RoutingPolicy2["PrivacyOptimized"] = "privacyOptimized";
})(RoutingPolicy || (RoutingPolicy = {}));
var HardwareAcceleration;
(function(HardwareAcceleration2) {
  HardwareAcceleration2["CPU"] = "cpu";
  HardwareAcceleration2["GPU"] = "gpu";
  HardwareAcceleration2["NeuralEngine"] = "neuralEngine";
  HardwareAcceleration2["NPU"] = "npu";
  HardwareAcceleration2["WebGPU"] = "webgpu";
  HardwareAcceleration2["WASM"] = "wasm";
})(HardwareAcceleration || (HardwareAcceleration = {}));
var ConfigurationSource;
(function(ConfigurationSource2) {
  ConfigurationSource2["Remote"] = "remote";
  ConfigurationSource2["Local"] = "local";
  ConfigurationSource2["Builtin"] = "builtin";
})(ConfigurationSource || (ConfigurationSource = {}));
var ModelStatus;
(function(ModelStatus2) {
  ModelStatus2["Registered"] = "registered";
  ModelStatus2["Downloading"] = "downloading";
  ModelStatus2["Downloaded"] = "downloaded";
  ModelStatus2["Loading"] = "loading";
  ModelStatus2["Loaded"] = "loaded";
  ModelStatus2["Error"] = "error";
})(ModelStatus || (ModelStatus = {}));
var DownloadStage;
(function(DownloadStage2) {
  DownloadStage2["Downloading"] = "downloading";
  DownloadStage2["Validating"] = "validating";
  DownloadStage2["Completed"] = "completed";
})(DownloadStage || (DownloadStage = {}));
var SDKEventType;
(function(SDKEventType2) {
  SDKEventType2["Initialization"] = "initialization";
  SDKEventType2["Configuration"] = "configuration";
  SDKEventType2["Generation"] = "generation";
  SDKEventType2["Model"] = "model";
  SDKEventType2["Voice"] = "voice";
  SDKEventType2["Storage"] = "storage";
  SDKEventType2["Framework"] = "framework";
  SDKEventType2["Device"] = "device";
  SDKEventType2["Error"] = "error";
  SDKEventType2["Performance"] = "performance";
  SDKEventType2["Network"] = "network";
})(SDKEventType || (SDKEventType = {}));
var AccelerationPreference;
(function(AccelerationPreference2) {
  AccelerationPreference2["Auto"] = "auto";
  AccelerationPreference2["WebGPU"] = "webgpu";
  AccelerationPreference2["CPU"] = "cpu";
})(AccelerationPreference || (AccelerationPreference = {}));

// node_modules/@runanywhere/web/dist/Foundation/SDKLogger.js
var LogLevel;
(function(LogLevel2) {
  LogLevel2[LogLevel2["Trace"] = 0] = "Trace";
  LogLevel2[LogLevel2["Debug"] = 1] = "Debug";
  LogLevel2[LogLevel2["Info"] = 2] = "Info";
  LogLevel2[LogLevel2["Warning"] = 3] = "Warning";
  LogLevel2[LogLevel2["Error"] = 4] = "Error";
  LogLevel2[LogLevel2["Fatal"] = 5] = "Fatal";
})(LogLevel || (LogLevel = {}));
var LOG_LEVEL_TO_RAC = {
  [LogLevel.Trace]: 0,
  [LogLevel.Debug]: 1,
  [LogLevel.Info]: 2,
  [LogLevel.Warning]: 3,
  [LogLevel.Error]: 4,
  [LogLevel.Fatal]: 5
};
var _SDKLogger = class _SDKLogger {
  constructor(category) {
    __publicField(this, "category");
    this.category = category;
  }
  static get level() {
    return _SDKLogger._level;
  }
  static set level(level) {
    _SDKLogger._level = level;
  }
  static get enabled() {
    return _SDKLogger._enabled;
  }
  static set enabled(value) {
    _SDKLogger._enabled = value;
  }
  trace(message) {
    this.log(LogLevel.Trace, message);
  }
  debug(message) {
    this.log(LogLevel.Debug, message);
  }
  info(message) {
    this.log(LogLevel.Info, message);
  }
  warning(message) {
    this.log(LogLevel.Warning, message);
  }
  error(message) {
    this.log(LogLevel.Error, message);
  }
  log(level, message) {
    if (!_SDKLogger._enabled || level < _SDKLogger._level) {
      return;
    }
    const prefix = `[RunAnywhere:${this.category}]`;
    switch (level) {
      case LogLevel.Trace:
      case LogLevel.Debug:
        console.debug(prefix, message);
        break;
      case LogLevel.Info:
        console.info(prefix, message);
        break;
      case LogLevel.Warning:
        console.warn(prefix, message);
        break;
      case LogLevel.Error:
      case LogLevel.Fatal:
        console.error(prefix, message);
        break;
    }
  }
};
__publicField(_SDKLogger, "_level", LogLevel.Info);
__publicField(_SDKLogger, "_enabled", true);
var SDKLogger = _SDKLogger;

// node_modules/@runanywhere/web/dist/Foundation/EventBus.js
var logger = new SDKLogger("EventBus");
var _EventBus = class _EventBus {
  constructor() {
    __publicField(this, "listeners", /* @__PURE__ */ new Map());
    __publicField(this, "wildcardListeners", /* @__PURE__ */ new Set());
  }
  static get shared() {
    if (!_EventBus._instance) {
      _EventBus._instance = new _EventBus();
    }
    return _EventBus._instance;
  }
  /**
   * Subscribe to events of a specific type.
   * @returns Unsubscribe function
   */
  on(eventType, listener) {
    const key = eventType;
    if (!this.listeners.has(key)) {
      this.listeners.set(key, /* @__PURE__ */ new Set());
    }
    const set = this.listeners.get(key);
    set.add(listener);
    return () => {
      set.delete(listener);
      if (set.size === 0) {
        this.listeners.delete(key);
      }
    };
  }
  /**
   * Subscribe to ALL events (wildcard).
   * @returns Unsubscribe function
   */
  onAny(listener) {
    this.wildcardListeners.add(listener);
    return () => {
      this.wildcardListeners.delete(listener);
    };
  }
  /**
   * Subscribe to events once (auto-unsubscribe after first event).
   */
  once(eventType, listener) {
    const unsubscribe = this.on(eventType, (event) => {
      unsubscribe();
      listener(event);
    });
    return unsubscribe;
  }
  /**
   * Emit an event.
   */
  emit(eventType, category, data) {
    const key = eventType;
    const payload = data ?? {};
    const envelope = {
      type: key,
      category,
      timestamp: Date.now(),
      data: payload
    };
    const specific = this.listeners.get(key);
    if (specific) {
      for (const listener of specific) {
        try {
          listener(payload);
        } catch (error) {
          logger.error(`Listener error for ${key}: ${error instanceof Error ? error.message : String(error)}`);
        }
      }
    }
    for (const listener of this.wildcardListeners) {
      try {
        listener(envelope);
      } catch (error) {
        logger.error(`Wildcard listener error: ${error instanceof Error ? error.message : String(error)}`);
      }
    }
  }
  /**
   * Remove all listeners.
   */
  removeAll() {
    this.listeners.clear();
    this.wildcardListeners.clear();
  }
  /** Reset singleton (for testing) */
  static reset() {
    if (_EventBus._instance) {
      _EventBus._instance.removeAll();
    }
    _EventBus._instance = null;
  }
};
__publicField(_EventBus, "_instance", null);
var EventBus = _EventBus;

// node_modules/@runanywhere/web/dist/Infrastructure/OPFSStorage.js
var logger2 = new SDKLogger("OPFSStorage");
var MODELS_DIR = "models";
var _OPFSStorage = class _OPFSStorage {
  constructor() {
    __publicField(this, "rootDir", null);
    __publicField(this, "modelsDir", null);
    __publicField(this, "_isAvailable", null);
  }
  /**
   * Check if OPFS is available in this browser.
   */
  static get isSupported() {
    return typeof navigator !== "undefined" && "storage" in navigator && "getDirectory" in (navigator.storage || {});
  }
  /**
   * Initialize OPFS storage. Must be called before other methods.
   *
   * @returns true if OPFS was initialized, false if not available
   */
  async initialize() {
    if (this._isAvailable !== null)
      return this._isAvailable;
    if (!_OPFSStorage.isSupported) {
      logger2.warning("OPFS not available in this browser. Models will use volatile MEMFS.");
      this._isAvailable = false;
      return false;
    }
    try {
      this.rootDir = await navigator.storage.getDirectory();
      this.modelsDir = await this.rootDir.getDirectoryHandle(MODELS_DIR, { create: true });
      this._isAvailable = true;
      logger2.info("OPFS storage initialized");
      return true;
    } catch (error) {
      const msg = error instanceof Error ? error.message : String(error);
      logger2.warning(`OPFS initialization failed: ${msg}`);
      this._isAvailable = false;
      return false;
    }
  }
  get isAvailable() {
    return this._isAvailable === true;
  }
  // ---------------------------------------------------------------------------
  // Core operations
  // ---------------------------------------------------------------------------
  /**
   * Save model data to OPFS.
   *
   * Supports nested paths: `saveModel('org/model/file.gguf', data)` creates
   * `models/org/model/` directories and writes `file.gguf`.
   *
   * @param key - Model identifier or nested path (used as filename / path)
   * @param data - Model file data
   */
  async saveModel(key, data) {
    if (!this.modelsDir)
      throw new Error("OPFS not initialized. Call initialize() first.");
    logger2.info(`Saving model to OPFS: ${key} (${(data.byteLength / 1024 / 1024).toFixed(1)} MB)`);
    const dir = await this.resolveParentDir(
      key,
      /* create */
      true
    );
    const filename = this.resolveFilename(key);
    const fileHandle = await dir.getFileHandle(filename, { create: true });
    const writable = await fileHandle.createWritable();
    try {
      await writable.write(data);
      await writable.close();
    } catch (writeError) {
      try {
        await writable.abort();
      } catch {
      }
      try {
        await dir.removeEntry(filename);
      } catch {
      }
      throw writeError;
    }
    logger2.info(`Model saved: ${key}`);
  }
  /**
   * Save model data to OPFS from a ReadableStream.
   * Streams data directly to disk without buffering the entire file in memory.
   *
   * @param key - Model identifier or nested path
   * @param stream - Readable stream of model data
   */
  async saveModelFromStream(key, stream) {
    if (!this.modelsDir)
      throw new Error("OPFS not initialized. Call initialize() first.");
    const dir = await this.resolveParentDir(
      key,
      /* create */
      true
    );
    const filename = this.resolveFilename(key);
    const fileHandle = await dir.getFileHandle(filename, { create: true });
    const writable = await fileHandle.createWritable();
    try {
      const reader = stream.getReader();
      while (true) {
        const { done, value } = await reader.read();
        if (done)
          break;
        await writable.write(value);
      }
      await writable.close();
      logger2.info(`Model streamed to OPFS: ${key}`);
    } catch (writeError) {
      try {
        await writable.abort();
      } catch {
      }
      try {
        await dir.removeEntry(filename);
      } catch {
      }
      throw writeError;
    }
  }
  /**
   * Load model data from OPFS.
   *
   * @param key - Model identifier or nested path
   * @returns Model data, or null if not found
   */
  async loadModel(key) {
    if (!this.modelsDir)
      return null;
    try {
      const dir = await this.resolveParentDir(
        key,
        /* create */
        false
      );
      const filename = this.resolveFilename(key);
      const fileHandle = await dir.getFileHandle(filename);
      const file = await fileHandle.getFile();
      logger2.info(`Loaded model from OPFS: ${key} (${(file.size / 1024 / 1024).toFixed(1)} MB)`);
      return await file.arrayBuffer();
    } catch {
      return null;
    }
  }
  /**
   * Load model data from OPFS as a ReadableStream.
   *
   * @param key - Model identifier or nested path
   * @returns Readable stream of the model data, or null if not found
   */
  async loadModelStream(key) {
    if (!this.modelsDir)
      return null;
    try {
      const dir = await this.resolveParentDir(
        key,
        /* create */
        false
      );
      const filename = this.resolveFilename(key);
      const fileHandle = await dir.getFileHandle(filename);
      const file = await fileHandle.getFile();
      logger2.info(`Loading model stream from OPFS: ${key} (${(file.size / 1024 / 1024).toFixed(1)} MB)`);
      return file.stream();
    } catch {
      return null;
    }
  }
  /**
   * Load model file object from OPFS without reading contents into memory.
   *
   * @param key - Model identifier or nested path
   * @returns File object, or null if not found
   */
  async loadModelFile(key) {
    if (!this.modelsDir)
      return null;
    try {
      const dir = await this.resolveParentDir(
        key,
        /* create */
        false
      );
      const filename = this.resolveFilename(key);
      const fileHandle = await dir.getFileHandle(filename);
      return await fileHandle.getFile();
    } catch {
      return null;
    }
  }
  /**
   * Check if a model exists in OPFS.
   *
   * @param key - Model identifier or nested path
   */
  async hasModel(key) {
    if (!this.modelsDir)
      return false;
    try {
      const dir = await this.resolveParentDir(
        key,
        /* create */
        false
      );
      const filename = this.resolveFilename(key);
      await dir.getFileHandle(filename);
      return true;
    } catch {
      return false;
    }
  }
  /**
   * Delete a model from OPFS.
   *
   * @param key - Model identifier or nested path
   */
  async deleteModel(key) {
    if (!this.modelsDir)
      return;
    try {
      const dir = await this.resolveParentDir(
        key,
        /* create */
        false
      );
      const filename = this.resolveFilename(key);
      await dir.removeEntry(filename);
      logger2.info(`Deleted model from OPFS: ${key}`);
    } catch {
    }
  }
  /**
   * Get the byte size of a stored file without reading it into memory.
   *
   * @param key - Model identifier or nested path
   * @returns File size in bytes, or null if the file doesn't exist
   */
  async getFileSize(key) {
    if (!this.modelsDir)
      return null;
    try {
      const dir = await this.resolveParentDir(
        key,
        /* create */
        false
      );
      const filename = this.resolveFilename(key);
      const fileHandle = await dir.getFileHandle(filename);
      const file = await fileHandle.getFile();
      return file.size;
    } catch {
      return null;
    }
  }
  // ---------------------------------------------------------------------------
  // Bulk operations
  // ---------------------------------------------------------------------------
  /**
   * List all stored models (top-level files only).
   */
  async listModels() {
    if (!this.modelsDir)
      return [];
    const models = [];
    for await (const [name, handle] of this.modelsDir.entries()) {
      if (handle.kind === "file") {
        const file = await handle.getFile();
        models.push({
          id: name,
          sizeBytes: file.size,
          lastModified: file.lastModified
        });
      }
    }
    return models;
  }
  /**
   * Get total storage usage.
   */
  async getStorageUsage() {
    var _a;
    if (!((_a = navigator.storage) == null ? void 0 : _a.estimate)) {
      return { usedBytes: 0, quotaBytes: 0 };
    }
    const estimate = await navigator.storage.estimate();
    return {
      usedBytes: estimate.usage ?? 0,
      quotaBytes: estimate.quota ?? 0
    };
  }
  /**
   * Clear all stored models.
   */
  async clearAll() {
    if (!this.rootDir)
      return;
    try {
      await this.rootDir.removeEntry(MODELS_DIR, { recursive: true });
      this.modelsDir = await this.rootDir.getDirectoryHandle(MODELS_DIR, { create: true });
      logger2.info("All OPFS models cleared");
    } catch (error) {
      const msg = error instanceof Error ? error.message : String(error);
      logger2.error(`Failed to clear OPFS: ${msg}`);
    }
  }
  /**
   * Save model metadata map to OPFS as a small JSON file.
   * Used for LRU tracking (lastUsedAt timestamps).
   */
  async saveMetadata(data) {
    if (!this.modelsDir)
      return;
    try {
      const json = JSON.stringify(data);
      const blob = new Blob([json], { type: "application/json" });
      const buf = await blob.arrayBuffer();
      const handle = await this.modelsDir.getFileHandle(_OPFSStorage.METADATA_FILENAME, { create: true });
      const writable = await handle.createWritable();
      await writable.write(buf);
      await writable.close();
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      logger2.warning(`Failed to save metadata: ${msg}`);
    }
  }
  /**
   * Load the persisted metadata map, or return an empty object.
   */
  async loadMetadata() {
    if (!this.modelsDir)
      return {};
    try {
      const handle = await this.modelsDir.getFileHandle(_OPFSStorage.METADATA_FILENAME);
      const file = await handle.getFile();
      const text = await file.text();
      return JSON.parse(text);
    } catch {
      return {};
    }
  }
  // ---------------------------------------------------------------------------
  // Path resolution helpers
  // ---------------------------------------------------------------------------
  /**
   * For a key that may contain `/`, traverse (and optionally create) the
   * intermediate directories and return the parent directory handle.
   *
   * For a flat key (no `/`) this returns `this.modelsDir` directly.
   */
  async resolveParentDir(key, create) {
    const dir = this.modelsDir;
    if (!key.includes("/"))
      return dir;
    const parts = key.split("/");
    let current = dir;
    for (let i = 0; i < parts.length - 1; i++) {
      current = await current.getDirectoryHandle(parts[i], { create });
    }
    return current;
  }
  /**
   * Extract the final filename segment from a key.
   *
   * For flat keys this returns a sanitized version of the whole key.
   * For nested keys (`org/model/file.gguf`) this returns the last segment
   * with only the filename portion sanitized (directory separators are handled
   * by `resolveParentDir`).
   */
  resolveFilename(key) {
    const raw = key.includes("/") ? key.split("/").pop() : key;
    return this.sanitizeFilename(raw);
  }
  /**
   * Sanitize a single filename segment.
   *
   * Only strips characters that are invalid in filenames. Keeps `.`, `-`, `_`,
   * and all alphanumeric characters. This is intentionally lenient compared to
   * the old implementation which also stripped `/` — directory separators are
   * now handled structurally by `resolveParentDir`.
   */
  sanitizeFilename(name) {
    return name.replace(/[<>:"/\\|?*\x00-\x1F]/g, "_");
  }
};
// ---------------------------------------------------------------------------
// Metadata persistence (lightweight JSON alongside model files)
// ---------------------------------------------------------------------------
__publicField(_OPFSStorage, "METADATA_FILENAME", "_metadata.json");
var OPFSStorage = _OPFSStorage;

// node_modules/@runanywhere/web/dist/Infrastructure/ModelRegistry.js
var HF_BASE = "https://huggingface.co";
function resolveModelDef(def) {
  const files = def.files ?? [];
  const baseUrl = def.repo ? `${HF_BASE}/${def.repo}/resolve/main` : void 0;
  if (def.artifactType === "archive") {
    const archiveUrl = def.url;
    if (!archiveUrl) {
      throw new Error(`Archive model '${def.id}' must specify a 'url' for the archive.`);
    }
    return {
      id: def.id,
      name: def.name,
      url: archiveUrl,
      framework: def.framework,
      modality: def.modality,
      memoryRequirement: def.memoryRequirement,
      isArchive: true
    };
  }
  const primaryUrl = def.url ?? `${baseUrl}/${files[0]}`;
  const additionalFiles = files.slice(1).map((filename) => ({
    url: baseUrl ? `${baseUrl}/${filename}` : filename,
    filename
  }));
  return {
    id: def.id,
    name: def.name,
    url: primaryUrl,
    framework: def.framework,
    modality: def.modality,
    memoryRequirement: def.memoryRequirement,
    ...additionalFiles.length > 0 ? { additionalFiles } : {}
  };
}
var ModelRegistry = class {
  constructor() {
    __publicField(this, "models", []);
    __publicField(this, "listeners", []);
  }
  // --- Registration ---
  /**
   * Register a catalog of models. Resolves compact definitions into full
   * ManagedModel entries.
   *
   * @returns The resolved models array (callers can use this for further checks).
   */
  registerModels(defs) {
    const resolved = defs.map(resolveModelDef);
    this.models = resolved.map((m) => ({ ...m, status: ModelStatus.Registered }));
    this.notifyListeners();
    EventBus.shared.emit("model.registered", SDKEventType.Model, { count: defs.length });
    return this.getModels();
  }
  /**
   * Add a single model to the registry without replacing existing ones.
   * Used for importing models via file picker or drag-and-drop.
   * If a model with the same ID already exists, this is a no-op.
   */
  addModel(model) {
    if (this.models.some((m) => m.id === model.id))
      return;
    this.models.push(model);
    this.notifyListeners();
  }
  // --- Queries ---
  getModels() {
    return [...this.models];
  }
  getModel(id) {
    return this.models.find((m) => m.id === id);
  }
  getModelsByCategory(category) {
    return this.models.filter((m) => m.modality === category);
  }
  getModelsByFramework(framework) {
    return this.models.filter((m) => m.framework === framework);
  }
  getLLMModels() {
    return this.models.filter((m) => m.modality === ModelCategory.Language);
  }
  getVLMModels() {
    return this.models.filter((m) => m.modality === ModelCategory.Multimodal);
  }
  getSTTModels() {
    return this.models.filter((m) => m.modality === ModelCategory.SpeechRecognition);
  }
  getTTSModels() {
    return this.models.filter((m) => m.modality === ModelCategory.SpeechSynthesis);
  }
  getVADModels() {
    return this.models.filter((m) => m.modality === ModelCategory.Audio);
  }
  // --- Status tracking ---
  updateModel(id, patch) {
    this.models = this.models.map((m) => m.id === id ? { ...m, ...patch } : m);
    this.notifyListeners();
  }
  // --- Listener / onChange pattern ---
  onChange(callback) {
    this.listeners.push(callback);
    return () => {
      this.listeners = this.listeners.filter((l) => l !== callback);
    };
  }
  notifyListeners() {
    for (const listener of this.listeners) {
      listener(this.getModels());
    }
  }
};

// node_modules/@runanywhere/web/dist/services/AnalyticsEmitter.js
var logger3 = new SDKLogger("AnalyticsEmitter");
var AnalyticsEmitterHolder = class {
  constructor() {
    __publicField(this, "_backend", null);
  }
  /** Register the concrete backend (called once by LlamaCppProvider). */
  registerBackend(backend) {
    this._backend = backend;
    logger3.info("Analytics emitter backend registered");
  }
  /** Remove the registered backend (called during cleanup). */
  removeBackend() {
    this._backend = null;
  }
  /** Whether a backend has been registered. */
  get hasBackend() {
    return this._backend !== null;
  }
  // -- STT -----------------------------------------------------------------
  emitSTTModelLoadCompleted(modelId, modelName, durationMs, framework) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitSTTModelLoadCompleted(modelId, modelName, durationMs, framework);
    });
  }
  emitSTTModelLoadFailed(modelId, errorCode, errorMessage) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitSTTModelLoadFailed(modelId, errorCode, errorMessage);
    });
  }
  emitSTTTranscriptionCompleted(transcriptionId, modelId, text, confidence, durationMs, audioLengthMs, audioSizeBytes, wordCount, realTimeFactor, language, sampleRate, framework) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitSTTTranscriptionCompleted(transcriptionId, modelId, text, confidence, durationMs, audioLengthMs, audioSizeBytes, wordCount, realTimeFactor, language, sampleRate, framework);
    });
  }
  emitSTTTranscriptionFailed(transcriptionId, modelId, errorCode, errorMessage) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitSTTTranscriptionFailed(transcriptionId, modelId, errorCode, errorMessage);
    });
  }
  // -- TTS -----------------------------------------------------------------
  emitTTSVoiceLoadCompleted(modelId, modelName, durationMs, framework) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitTTSVoiceLoadCompleted(modelId, modelName, durationMs, framework);
    });
  }
  emitTTSVoiceLoadFailed(modelId, errorCode, errorMessage) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitTTSVoiceLoadFailed(modelId, errorCode, errorMessage);
    });
  }
  emitTTSSynthesisCompleted(synthesisId, modelId, characterCount, audioDurationMs, audioSizeBytes, processingDurationMs, charactersPerSecond, sampleRate, framework) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitTTSSynthesisCompleted(synthesisId, modelId, characterCount, audioDurationMs, audioSizeBytes, processingDurationMs, charactersPerSecond, sampleRate, framework);
    });
  }
  emitTTSSynthesisFailed(synthesisId, modelId, errorCode, errorMessage) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitTTSSynthesisFailed(synthesisId, modelId, errorCode, errorMessage);
    });
  }
  // -- VAD -----------------------------------------------------------------
  emitVADSpeechStarted() {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitVADSpeechStarted();
    });
  }
  emitVADSpeechEnded(speechDurationMs, energyLevel) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitVADSpeechEnded(speechDurationMs, energyLevel);
    });
  }
  // -- Download ------------------------------------------------------------
  emitModelDownloadStarted(modelId) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitModelDownloadStarted(modelId);
    });
  }
  emitModelDownloadCompleted(modelId, fileSizeBytes, durationMs) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitModelDownloadCompleted(modelId, fileSizeBytes, durationMs);
    });
  }
  emitModelDownloadFailed(modelId, errorMessage) {
    this.safe(() => {
      var _a;
      return (_a = this._backend) == null ? void 0 : _a.emitModelDownloadFailed(modelId, errorMessage);
    });
  }
  // -- Internal ------------------------------------------------------------
  /** Swallow any error from telemetry — must never crash the host app. */
  safe(fn) {
    try {
      fn();
    } catch {
    }
  }
};
var AnalyticsEmitter = new AnalyticsEmitterHolder();

// node_modules/@runanywhere/web/dist/Infrastructure/ModelDownloader.js
function validateModelUrl(url) {
  let parsed;
  try {
    parsed = new URL(url);
  } catch {
    throw new Error(`Invalid model URL: ${url}`);
  }
  const isLocalhost = parsed.hostname === "localhost" || parsed.hostname === "127.0.0.1" || parsed.hostname === "[::1]";
  if (parsed.protocol === "http:" && !isLocalhost) {
    throw new Error(`Model URL must use HTTPS (got HTTP for ${parsed.hostname}). HTTP is only allowed for localhost during development.`);
  }
  if (parsed.protocol !== "https:" && parsed.protocol !== "http:") {
    throw new Error(`Model URL has unsupported protocol: ${parsed.protocol}`);
  }
  const blockedPatterns = [
    /^10\./,
    /^172\.(1[6-9]|2\d|3[0-1])\./,
    /^192\.168\./,
    /^169\.254\./,
    /^0\./
  ];
  if (!isLocalhost) {
    for (const pattern of blockedPatterns) {
      if (pattern.test(parsed.hostname)) {
        throw new Error(`Model URL points to private network address: ${parsed.hostname}`);
      }
    }
  }
}
var logger4 = new SDKLogger("ModelDownloader");
var ModelDownloader = class {
  constructor(registry, storage) {
    __publicField(this, "storage");
    __publicField(this, "registry");
    /**
     * Optional local filesystem storage. When configured, models are saved
     * to the user's chosen directory instead of OPFS. Set via setLocalFileStorage().
     */
    __publicField(this, "localFileStorage", null);
    /**
     * In-memory fallback cache for models that were downloaded successfully
     * but failed to persist to OPFS (e.g. storage quota exceeded).
     * Keyed by modelId/file key. Cleared once the data is consumed by loadFromOPFS.
     */
    __publicField(this, "memoryCache", /* @__PURE__ */ new Map());
    this.registry = registry;
    this.storage = storage;
  }
  /**
   * Set the local file storage backend.
   * When configured and ready, models are saved/loaded from the local filesystem first.
   */
  setLocalFileStorage(storage) {
    this.localFileStorage = storage;
  }
  // ---------------------------------------------------------------------------
  // Public API
  // ---------------------------------------------------------------------------
  /**
   * Check whether a model will fit in OPFS without eviction.
   *
   * Uses `navigator.storage.estimate()` for available space and compares
   * against the model's total size (primary + additional files).
   * If the model won't fit, returns eviction candidates sorted by LRU.
   *
   * @param model       - The model to check
   * @param metadata    - LRU metadata map (lastUsedAt per model)
   * @param loadedModelId - Currently loaded model ID (excluded from eviction)
   */
  async checkStorageQuota(model, metadata, loadedModelId) {
    var _a;
    const { usedBytes, quotaBytes } = await this.storage.getStorageUsage();
    const availableBytes = Math.max(0, quotaBytes - usedBytes);
    const neededBytes = model.memoryRequirement ?? 0;
    if (availableBytes >= neededBytes) {
      return { fits: true, availableBytes, neededBytes, evictionCandidates: [] };
    }
    const stored = await this.storage.listModels();
    const keepBase = model.id.split("__")[0];
    const candidates = [];
    for (const s of stored) {
      const storedBase = s.id.split("__")[0];
      if (storedBase === keepBase)
        continue;
      if (loadedModelId && s.id === loadedModelId)
        continue;
      if (s.id === "_metadata.json")
        continue;
      const registered = this.registry.getModel(s.id) ?? this.registry.getModel(storedBase);
      candidates.push({
        id: storedBase,
        name: (registered == null ? void 0 : registered.name) ?? s.id,
        sizeBytes: s.sizeBytes,
        lastUsedAt: ((_a = metadata[storedBase]) == null ? void 0 : _a.lastUsedAt) ?? s.lastModified
      });
    }
    const deduped = /* @__PURE__ */ new Map();
    for (const c of candidates) {
      const existing = deduped.get(c.id);
      if (existing) {
        existing.sizeBytes += c.sizeBytes;
      } else {
        deduped.set(c.id, { ...c });
      }
    }
    const sorted = [...deduped.values()].sort((a, b) => a.lastUsedAt - b.lastUsedAt);
    return { fits: false, availableBytes, neededBytes, evictionCandidates: sorted };
  }
  /**
   * Download a model (and any additional companion files).
   * Handles both single-file and multi-file models.
   */
  async downloadModel(modelId) {
    var _a;
    const model = this.registry.getModel(modelId);
    if (!model)
      return;
    this.registry.updateModel(modelId, { status: ModelStatus.Downloading, downloadProgress: 0 });
    EventBus.shared.emit("model.downloadStarted", SDKEventType.Model, { modelId, url: model.url });
    AnalyticsEmitter.emitModelDownloadStarted(modelId);
    try {
      const totalFiles = 1 + (((_a = model.additionalFiles) == null ? void 0 : _a.length) ?? 0);
      let totalBytesDownloaded = 0;
      let totalBytesExpected = 0;
      const primaryProgressCb = (progress, bytesDown, bytesTotal) => {
        totalBytesDownloaded = bytesDown;
        totalBytesExpected = bytesTotal * totalFiles;
        const overallProgress = progress / totalFiles;
        this.registry.updateModel(modelId, { downloadProgress: overallProgress });
        this.emitDownloadProgress({
          modelId,
          stage: DownloadStage.Downloading,
          progress: overallProgress,
          bytesDownloaded: totalBytesDownloaded,
          totalBytes: totalBytesExpected,
          currentFile: model.url.split("/").pop(),
          filesCompleted: 0,
          filesTotal: totalFiles
        });
      };
      let primarySize = await this.downloadAndStoreStreaming(model.url, modelId, primaryProgressCb);
      if (primarySize === null) {
        const primaryData = await this.downloadFile(model.url, primaryProgressCb);
        await this.storeInOPFS(modelId, primaryData);
        primarySize = primaryData.length;
      }
      if (model.additionalFiles && model.additionalFiles.length > 0) {
        for (let i = 0; i < model.additionalFiles.length; i++) {
          const file = model.additionalFiles[i];
          const fileKey = this.additionalFileKey(modelId, file.filename);
          const fileProgressCb = (progress, bytesDown, bytesTotal) => {
            const baseProgress = (1 + i) / totalFiles;
            const fileProgress = progress / totalFiles;
            const overallProgress = baseProgress + fileProgress;
            this.registry.updateModel(modelId, { downloadProgress: overallProgress });
            this.emitDownloadProgress({
              modelId,
              stage: DownloadStage.Downloading,
              progress: overallProgress,
              bytesDownloaded: bytesDown,
              totalBytes: bytesTotal,
              currentFile: file.filename,
              filesCompleted: 1 + i,
              filesTotal: totalFiles
            });
          };
          const streamedSize = await this.downloadAndStoreStreaming(file.url, fileKey, fileProgressCb);
          if (streamedSize === null) {
            const fileData = await this.downloadFile(file.url, fileProgressCb);
            await this.storeInOPFS(fileKey, fileData);
          }
        }
      }
      this.emitDownloadProgress({
        modelId,
        stage: DownloadStage.Validating,
        progress: 0.95,
        bytesDownloaded: totalBytesDownloaded,
        totalBytes: totalBytesExpected,
        filesCompleted: totalFiles,
        filesTotal: totalFiles
      });
      let totalSize = primarySize;
      if (model.additionalFiles) {
        for (const file of model.additionalFiles) {
          const fileKey = this.additionalFileKey(modelId, file.filename);
          const size = await this.storage.getFileSize(fileKey);
          if (size !== null)
            totalSize += size;
        }
      }
      this.registry.updateModel(modelId, {
        status: ModelStatus.Downloaded,
        downloadProgress: 1,
        sizeBytes: totalSize
      });
      this.emitDownloadProgress({
        modelId,
        stage: DownloadStage.Completed,
        progress: 1,
        bytesDownloaded: totalSize,
        totalBytes: totalSize,
        filesCompleted: totalFiles,
        filesTotal: totalFiles
      });
      EventBus.shared.emit("model.downloadCompleted", SDKEventType.Model, { modelId, sizeBytes: totalSize });
      AnalyticsEmitter.emitModelDownloadCompleted(modelId, totalSize, 0);
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      this.registry.updateModel(modelId, { status: ModelStatus.Error, error: message });
      EventBus.shared.emit("model.downloadFailed", SDKEventType.Model, { modelId, error: message });
      AnalyticsEmitter.emitModelDownloadFailed(modelId, message);
    }
  }
  // ---------------------------------------------------------------------------
  // OPFS delegation helpers (used by ModelManager for load-time operations)
  // ---------------------------------------------------------------------------
  /**
   * Download a file from a URL with optional progress callback.
   * Exposed so ModelManager can use it for on-demand file downloads during load.
   *
   * URLs are validated before fetching to prevent SSRF and enforce HTTPS.
   */
  async downloadFile(url, onProgress) {
    var _a;
    validateModelUrl(url);
    const response = await fetch(url);
    if (!response.ok)
      throw new Error(`HTTP ${response.status} for ${url}`);
    const total = Number(response.headers.get("content-length") || 0);
    const reader = (_a = response.body) == null ? void 0 : _a.getReader();
    if (!reader)
      throw new Error("No response body");
    const chunks = [];
    let received = 0;
    while (true) {
      const { done, value } = await reader.read();
      if (done)
        break;
      chunks.push(value);
      received += value.length;
      const progress = total > 0 ? received / total : 0;
      onProgress == null ? void 0 : onProgress(progress, received, total);
    }
    const data = new Uint8Array(received);
    let offset = 0;
    for (const chunk of chunks) {
      data.set(chunk, offset);
      offset += chunk.length;
    }
    return data;
  }
  /**
   * Download a file and stream it directly to persistent storage (OPFS or local FS)
   * without buffering the entire payload in memory.
   *
   * Returns the total bytes downloaded. Falls back to buffered download + store
   * if streaming write is not supported or fails.
   *
   * @returns Total bytes written, or null if streaming was not possible.
   */
  async downloadAndStoreStreaming(url, storageKey, onProgress) {
    var _a;
    validateModelUrl(url);
    const response = await fetch(url);
    if (!response.ok)
      throw new Error(`HTTP ${response.status} for ${url}`);
    if (!response.body)
      return null;
    const total = Number(response.headers.get("content-length") || 0);
    let received = 0;
    const progressTransform = new TransformStream({
      transform: (chunk, controller) => {
        received += chunk.length;
        onProgress == null ? void 0 : onProgress(total > 0 ? received / total : 0, received, total);
        controller.enqueue(chunk);
      }
    });
    const storageStream = response.body.pipeThrough(progressTransform);
    try {
      if ((_a = this.localFileStorage) == null ? void 0 : _a.isReady) {
        await this.localFileStorage.saveModelFromStream(storageKey, storageStream);
        logger4.info(`Streamed ${storageKey} to local storage (${(received / 1024 / 1024).toFixed(1)} MB)`);
        return received;
      }
      await this.storage.saveModelFromStream(storageKey, storageStream);
      logger4.info(`Streamed ${storageKey} to OPFS (${(received / 1024 / 1024).toFixed(1)} MB)`);
      return received;
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      logger4.warning(`Streaming store failed for "${storageKey}": ${msg}, will fall back to buffered download`);
      return null;
    }
  }
  /** Store data, preferring local filesystem when available, then OPFS, then memory cache. */
  async storeInOPFS(key, data) {
    var _a;
    const sizeMB = (data.length / 1024 / 1024).toFixed(1);
    if ((_a = this.localFileStorage) == null ? void 0 : _a.isReady) {
      try {
        await this.localFileStorage.saveModel(key, data.buffer);
        logger4.info(`Stored ${key} in local storage (${sizeMB} MB)`);
        this.memoryCache.delete(key);
        return;
      } catch (err) {
        const msg = err instanceof Error ? err.message : String(err);
        logger4.warning(`Local storage write failed for "${key}": ${msg}, falling back to OPFS`);
      }
    }
    try {
      await this.storage.saveModel(key, data.buffer);
      logger4.info(`Stored ${key} in OPFS (${sizeMB} MB)`);
      this.memoryCache.delete(key);
      return;
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      const isQuota = msg.toLowerCase().includes("quota");
      if (!isQuota) {
        logger4.warning(`OPFS store failed for "${key}": ${msg}`);
        logger4.info(`Caching "${key}" in memory (${sizeMB} MB) for current session`);
        this.memoryCache.set(key, data);
        return;
      }
      logger4.warning(`OPFS quota exceeded for "${key}" (${sizeMB} MB), evicting old models...`);
      await this.evictOPFSModels(key, data.length);
    }
    try {
      await this.storage.saveModel(key, data.buffer);
      logger4.info(`Stored ${key} in OPFS after eviction (${sizeMB} MB)`);
      this.memoryCache.delete(key);
    } catch (retryErr) {
      const retryMsg = retryErr instanceof Error ? retryErr.message : String(retryErr);
      logger4.warning(`OPFS store still failed after eviction for "${key}": ${retryMsg}`);
      logger4.info(`Caching "${key}" in memory (${sizeMB} MB) for current session`);
      this.memoryCache.set(key, data);
    }
  }
  /**
   * Evict old models from OPFS to free space for a new model.
   * Deletes models sorted by oldest-first until enough space is freed,
   * skipping the model being stored AND any sibling files that belong
   * to the same model (e.g. main model file and its mmproj companion).
   *
   * Sibling detection: files are siblings if one key is a prefix of
   * the other, separated by "__" (e.g. "modelA" and "modelA__mmproj-...").
   */
  async evictOPFSModels(keepKey, neededBytes) {
    const stored = await this.storage.listModels();
    if (stored.length === 0)
      return;
    const keepBase = keepKey.split("__")[0];
    stored.sort((a, b) => a.lastModified - b.lastModified);
    let freedBytes = 0;
    for (const model of stored) {
      if (model.id === keepKey)
        continue;
      if (model.id === "_metadata.json")
        continue;
      const storedBase = model.id.split("__")[0];
      if (storedBase === keepBase)
        continue;
      const sizeMBEvict = (model.sizeBytes / 1024 / 1024).toFixed(1);
      logger4.info(`Evicting "${model.id}" (${sizeMBEvict} MB) from OPFS`);
      await this.storage.deleteModel(model.id);
      const registered = this.registry.getModel(model.id);
      if (registered && registered.status === ModelStatus.Downloaded) {
        this.registry.updateModel(model.id, { status: ModelStatus.Registered });
      }
      EventBus.shared.emit("model.evicted", SDKEventType.Storage, {
        modelId: model.id,
        modelName: (registered == null ? void 0 : registered.name) ?? model.id,
        freedBytes: model.sizeBytes
      });
      freedBytes += model.sizeBytes;
      if (freedBytes >= neededBytes) {
        logger4.info(`Evicted ${(freedBytes / 1024 / 1024).toFixed(1)} MB, should have room now`);
        break;
      }
    }
  }
  /**
   * Store a file from a ReadableStream, avoiding loading the entire file into memory.
   * Priority: local filesystem > OPFS. Falls back to buffered write if streaming not supported.
   */
  async storeStreamInOPFS(key, stream) {
    var _a;
    if ((_a = this.localFileStorage) == null ? void 0 : _a.isReady) {
      try {
        await this.localFileStorage.saveModelFromStream(key, stream);
        return;
      } catch (err) {
        const msg = err instanceof Error ? err.message : String(err);
        logger4.warning(`Local storage stream write failed for "${key}": ${msg}, falling back to OPFS`);
      }
    }
    try {
      await this.storage.saveModelFromStream(key, stream);
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      logger4.warning(`OPFS stream store failed for "${key}": ${msg}`);
      throw err;
    }
  }
  /** Load data from storage. Priority: local filesystem > OPFS > memory cache. */
  async loadFromOPFS(key) {
    var _a;
    if ((_a = this.localFileStorage) == null ? void 0 : _a.isReady) {
      const localBuffer = await this.localFileStorage.loadModel(key);
      if (localBuffer && localBuffer.byteLength > 0) {
        const sizeMB = localBuffer.byteLength / 1024 / 1024;
        logger4.debug(`Loading ${key} from local storage (${sizeMB.toFixed(1)} MB)`);
        return new Uint8Array(localBuffer);
      }
    }
    const buffer = await this.storage.loadModel(key);
    if (buffer && buffer.byteLength > 0) {
      const sizeMB = buffer.byteLength / 1024 / 1024;
      logger4.debug(`Loading ${key} from OPFS (${sizeMB.toFixed(1)} MB)`);
      return new Uint8Array(buffer);
    }
    if (buffer && buffer.byteLength === 0) {
      logger4.warning(`OPFS entry for "${key}" is 0 bytes (corrupted), deleting`);
      await this.deleteFromOPFS(key);
    }
    const cached = this.memoryCache.get(key);
    if (cached) {
      const sizeMB = cached.length / 1024 / 1024;
      logger4.debug(`Loading ${key} from memory cache (${sizeMB.toFixed(1)} MB) — not persisted to OPFS`);
      return cached;
    }
    return null;
  }
  /** Load data from storage as a ReadableStream. Priority: local filesystem > OPFS > memory cache. */
  async loadStreamFromOPFS(key) {
    var _a;
    if ((_a = this.localFileStorage) == null ? void 0 : _a.isReady) {
      const localStream = await this.localFileStorage.loadModelStream(key);
      if (localStream) {
        logger4.debug(`Loading ${key} stream from local storage`);
        return localStream;
      }
    }
    const opfsStream = await this.storage.loadModelStream(key);
    if (opfsStream) {
      logger4.debug(`Loading ${key} stream from OPFS`);
      return opfsStream;
    }
    const cached = this.memoryCache.get(key);
    if (cached) {
      const sizeMB = cached.length / 1024 / 1024;
      logger4.debug(`Loading ${key} stream from memory cache (${sizeMB.toFixed(1)} MB)`);
      return new ReadableStream({
        start(controller) {
          controller.enqueue(cached);
          controller.close();
        }
      });
    }
    return null;
  }
  /** Load file object from storage (local FS or OPFS) without reading into memory. */
  async loadModelFile(key) {
    var _a;
    if ((_a = this.localFileStorage) == null ? void 0 : _a.isReady) {
      const file = await this.localFileStorage.loadModelFile(key);
      if (file)
        return file;
    }
    return this.storage.loadModelFile(key);
  }
  /** Check existence in local storage, OPFS, or in-memory cache. */
  async existsInOPFS(key) {
    var _a;
    if ((_a = this.localFileStorage) == null ? void 0 : _a.isReady) {
      const localExists = await this.localFileStorage.hasModel(key);
      if (localExists)
        return true;
    }
    if (this.memoryCache.has(key))
      return true;
    return this.storage.hasModel(key);
  }
  /** Check if data exists in actual OPFS storage (NOT memory cache). */
  async existsInActualOPFS(key) {
    return this.storage.hasModel(key);
  }
  /** Delete from all storage backends (local filesystem, OPFS, memory cache). */
  async deleteFromOPFS(key) {
    var _a;
    this.memoryCache.delete(key);
    if ((_a = this.localFileStorage) == null ? void 0 : _a.isReady) {
      await this.localFileStorage.deleteModel(key);
    }
    await this.storage.deleteModel(key);
  }
  /** Get file size from storage without reading into memory. */
  async getOPFSFileSize(key) {
    var _a;
    if ((_a = this.localFileStorage) == null ? void 0 : _a.isReady) {
      const localSize = await this.localFileStorage.getFileSize(key);
      if (localSize !== null)
        return localSize;
    }
    return this.storage.getFileSize(key);
  }
  // ---------------------------------------------------------------------------
  // Helpers
  // ---------------------------------------------------------------------------
  /**
   * Build an OPFS key for additional files (e.g., mmproj).
   * Uses `__` separator to avoid name collisions between
   * a primary model FILE and a directory with the same name.
   */
  additionalFileKey(modelId, filename) {
    return `${modelId}__${filename}`;
  }
  /** Emit a structured download progress event via EventBus */
  emitDownloadProgress(progress) {
    EventBus.shared.emit("model.downloadProgress", SDKEventType.Model, {
      modelId: progress.modelId,
      progress: progress.progress,
      bytesDownloaded: progress.bytesDownloaded,
      totalBytes: progress.totalBytes,
      stage: progress.stage
    });
  }
};

// node_modules/@runanywhere/web/dist/Infrastructure/ModelFileInference.js
function inferModelFromFilename(filename) {
  const ext = getExtension(filename);
  const baseName = stripExtension(filename);
  const lowerName = baseName.toLowerCase();
  if (ext === "gguf") {
    return {
      id: sanitizeId(baseName),
      name: humanizeName(baseName),
      category: ModelCategory.Language,
      framework: LLMFramework.LlamaCpp
    };
  }
  if (ext === "onnx") {
    if (lowerName.includes("vad") || lowerName.includes("silero_vad")) {
      return {
        id: sanitizeId(baseName),
        name: humanizeName(baseName),
        category: ModelCategory.Audio,
        framework: LLMFramework.ONNX
      };
    }
    if (lowerName.includes("tts") || lowerName.includes("piper") || lowerName.includes("vits")) {
      return {
        id: sanitizeId(baseName),
        name: humanizeName(baseName),
        category: ModelCategory.SpeechSynthesis,
        framework: LLMFramework.ONNX
      };
    }
    if (lowerName.includes("whisper") || lowerName.includes("stt") || lowerName.includes("paraformer") || lowerName.includes("zipformer")) {
      return {
        id: sanitizeId(baseName),
        name: humanizeName(baseName),
        category: ModelCategory.SpeechRecognition,
        framework: LLMFramework.ONNX
      };
    }
    return {
      id: sanitizeId(baseName),
      name: humanizeName(baseName),
      category: ModelCategory.Language,
      framework: LLMFramework.ONNX
    };
  }
  return {
    id: sanitizeId(baseName),
    name: humanizeName(baseName),
    category: ModelCategory.Language,
    framework: LLMFramework.LlamaCpp
  };
}
function getExtension(filename) {
  const lower = filename.toLowerCase();
  if (lower.endsWith(".tar.gz"))
    return "tar.gz";
  if (lower.endsWith(".tar.bz2"))
    return "tar.bz2";
  const lastDot = filename.lastIndexOf(".");
  return lastDot >= 0 ? filename.slice(lastDot + 1).toLowerCase() : "";
}
function stripExtension(filename) {
  const lower = filename.toLowerCase();
  if (lower.endsWith(".tar.gz"))
    return filename.slice(0, -7);
  if (lower.endsWith(".tar.bz2"))
    return filename.slice(0, -8);
  const lastDot = filename.lastIndexOf(".");
  return lastDot >= 0 ? filename.slice(0, lastDot) : filename;
}
function sanitizeId(name) {
  return name.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
}
function humanizeName(baseName) {
  return baseName.replace(/_/g, " ");
}

// node_modules/@runanywhere/web/dist/Infrastructure/ModelManager.js
var logger5 = new SDKLogger("ModelManager");
var ModelManagerImpl = class {
  constructor() {
    __publicField(this, "registry", new ModelRegistry());
    __publicField(this, "storage", new OPFSStorage());
    __publicField(this, "downloader");
    /**
     * Tracks loaded models per category — allows STT + LLM + TTS simultaneously
     * for the voice pipeline. Key = ModelCategory, Value = model id.
     */
    __publicField(this, "loadedByCategory", /* @__PURE__ */ new Map());
    /** LRU metadata: lastUsedAt timestamps persisted in OPFS */
    __publicField(this, "metadata", {});
    /** Pluggable VLM loader (set by the app via setVLMLoader) */
    __publicField(this, "vlmLoader", null);
    /** Pluggable model loaders — registered by backend providers */
    __publicField(this, "llmLoader", null);
    __publicField(this, "sttLoader", null);
    __publicField(this, "ttsLoader", null);
    __publicField(this, "vadLoader", null);
    this.downloader = new ModelDownloader(this.registry, this.storage);
    this.initStorage();
    this.requestPersistentStorage();
  }
  async initStorage() {
    await this.storage.initialize();
  }
  // --- Registration API ---
  registerModels(models) {
    this.registry.registerModels(models);
    this.refreshDownloadStatus();
  }
  setVLMLoader(loader) {
    this.vlmLoader = loader;
  }
  setLLMLoader(loader) {
    this.llmLoader = loader;
  }
  setSTTLoader(loader) {
    this.sttLoader = loader;
  }
  setTTSLoader(loader) {
    this.ttsLoader = loader;
  }
  setVADLoader(loader) {
    this.vadLoader = loader;
  }
  /** Expose the downloader for backend packages that need file operations. */
  getDownloader() {
    return this.downloader;
  }
  /** Set the local file storage backend for persistent model storage. */
  setLocalFileStorage(storage) {
    this.downloader.setLocalFileStorage(storage);
  }
  // --- Internal init ---
  async requestPersistentStorage() {
    var _a;
    try {
      if ((_a = navigator.storage) == null ? void 0 : _a.persist) {
        const persisted = await navigator.storage.persist();
        if (persisted) {
          logger5.info("Persistent storage: granted");
        } else {
          logger5.debug("Persistent storage: denied (expected on first visit)");
        }
      }
    } catch {
    }
  }
  async refreshDownloadStatus() {
    await this.storage.initialize();
    this.metadata = await this.storage.loadMetadata();
    for (const model of this.registry.getModels()) {
      if (model.status !== ModelStatus.Registered)
        continue;
      try {
        const size = await this.downloader.getOPFSFileSize(model.id);
        if (size !== null && size > 0) {
          this.registry.updateModel(model.id, { status: ModelStatus.Downloaded, sizeBytes: size });
          if (!this.metadata[model.id]) {
            const stored = await this.storage.listModels();
            const entry = stored.find((s) => s.id === model.id);
            this.metadata[model.id] = {
              lastUsedAt: (entry == null ? void 0 : entry.lastModified) ?? Date.now(),
              sizeBytes: size
            };
          }
        }
      } catch {
      }
    }
    await this.storage.saveMetadata(this.metadata);
  }
  // --- Queries ---
  getModels() {
    return this.registry.getModels();
  }
  getModelsByCategory(category) {
    return this.registry.getModelsByCategory(category);
  }
  getModelsByFramework(framework) {
    return this.registry.getModelsByFramework(framework);
  }
  getLLMModels() {
    return this.registry.getLLMModels();
  }
  getVLMModels() {
    return this.registry.getVLMModels();
  }
  getSTTModels() {
    return this.registry.getSTTModels();
  }
  getTTSModels() {
    return this.registry.getTTSModels();
  }
  getVADModels() {
    return this.registry.getVADModels();
  }
  getLoadedModel(category) {
    if (category) {
      const id = this.loadedByCategory.get(category);
      return id ? this.registry.getModel(id) ?? null : null;
    }
    return this.registry.getModels().find((m) => m.status === ModelStatus.Loaded) ?? null;
  }
  getLoadedModelId(category) {
    var _a;
    if (category) {
      return this.loadedByCategory.get(category) ?? null;
    }
    return ((_a = this.registry.getModels().find((m) => m.status === ModelStatus.Loaded)) == null ? void 0 : _a.id) ?? null;
  }
  areAllLoaded(categories) {
    return categories.every((c) => this.loadedByCategory.has(c));
  }
  async ensureLoaded(category, options) {
    const loaded = this.getLoadedModel(category);
    if (loaded)
      return loaded;
    const models = this.getModels();
    const downloaded = models.find((m) => m.modality === category && m.status === ModelStatus.Downloaded);
    if (!downloaded)
      return null;
    await this.loadModel(downloaded.id, options);
    return this.getLoadedModel(category);
  }
  // --- Download ---
  async checkDownloadFit(modelId) {
    const model = this.registry.getModel(modelId);
    if (!model)
      return { fits: true, availableBytes: 0, neededBytes: 0, evictionCandidates: [] };
    const loadedId = this.loadedByCategory.get(model.modality ?? ModelCategory.Language);
    return this.downloader.checkStorageQuota(model, this.metadata, loadedId ?? void 0);
  }
  async downloadModel(modelId) {
    return this.downloader.downloadModel(modelId);
  }
  // --- Model Import (file picker / drag-drop) ---
  /**
   * Import a model from a user-provided File (via picker or drag-and-drop).
   * Stores the file in the active storage backend and registers it as downloaded.
   * If the model isn't already in the catalog, auto-registers it based on filename.
   *
   * @param file - The File object from file picker or drag-drop
   * @param modelId - Optional: associate with an existing registered model
   * @returns The model ID (existing or auto-generated)
   */
  async importModel(file, modelId) {
    let id = modelId ?? sanitizeId(file.name.replace(/\.[^.]+$/, ""));
    if (!this.registry.getModel(id)) {
      const meta = inferModelFromFilename(file.name);
      this.registry.addModel({
        id: meta.id,
        name: meta.name,
        url: "",
        modality: meta.category,
        framework: meta.framework,
        status: ModelStatus.Registered
      });
      id = meta.id;
    }
    logger5.info(`Importing model from file: ${file.name} (${(file.size / 1024 / 1024).toFixed(1)} MB) -> ${id}`);
    if (typeof file.stream === "function") {
      await this.downloader.storeStreamInOPFS(id, file.stream());
    } else {
      const data = new Uint8Array(await file.arrayBuffer());
      await this.downloader.storeInOPFS(id, data);
    }
    const sizeBytes = file.size;
    this.registry.updateModel(id, {
      status: ModelStatus.Downloaded,
      sizeBytes
    });
    this.touchLastUsed(id, sizeBytes);
    EventBus.shared.emit("model.imported", SDKEventType.Model, {
      modelId: id,
      filename: file.name,
      sizeBytes
    });
    logger5.info(`Model imported: ${id} (${(sizeBytes / 1024 / 1024).toFixed(1)} MB)`);
    return id;
  }
  // --- Model loading orchestration ---
  async loadModel(modelId, options) {
    const model = this.registry.getModel(modelId);
    if (!model || model.status !== ModelStatus.Downloaded && model.status !== ModelStatus.Registered)
      return false;
    const category = model.modality ?? ModelCategory.Language;
    if (options == null ? void 0 : options.coexist) {
      const currentId = this.loadedByCategory.get(category);
      if (currentId && currentId !== modelId) {
        logger5.info(`Swapping ${category} model: ${currentId} → ${modelId}`);
        await this.unloadModelByCategory(category);
      }
    } else {
      await this.unloadAll(modelId);
    }
    this.registry.updateModel(modelId, { status: ModelStatus.Loading });
    EventBus.shared.emit("model.loadStarted", SDKEventType.Model, { modelId, category });
    try {
      if (model.modality === ModelCategory.Multimodal) {
        await this.loadVLMModel(model, modelId);
      } else if (model.modality === ModelCategory.SpeechRecognition) {
        const data = await this.downloader.loadFromOPFS(modelId);
        if (!data)
          throw new Error("Model not downloaded — please download the model first.");
        await this.loadSTTModel(model, data);
      } else if (model.modality === ModelCategory.SpeechSynthesis) {
        const data = await this.downloader.loadFromOPFS(modelId);
        if (!data)
          throw new Error("Model not downloaded — please download the model first.");
        await this.loadTTSModel(model, data);
      } else if (model.modality === ModelCategory.Audio) {
        const data = await this.downloader.loadFromOPFS(modelId);
        if (!data)
          throw new Error("Model not downloaded — please download the model first.");
        await this.loadVADModel(model, data);
      } else {
        const file = await this.downloader.loadModelFile(modelId);
        let dataStream;
        let data;
        if (!file) {
          dataStream = await this.downloader.loadStreamFromOPFS(modelId) ?? void 0;
          if (!dataStream) {
            data = await this.downloader.loadFromOPFS(modelId) ?? void 0;
          }
        }
        if (!file && !dataStream && !data)
          throw new Error("Model not downloaded — please download the model first.");
        await this.loadLLMModel(model, modelId, data, dataStream, file ?? void 0);
      }
      this.loadedByCategory.set(category, modelId);
      this.registry.updateModel(modelId, { status: ModelStatus.Loaded });
      EventBus.shared.emit("model.loadCompleted", SDKEventType.Model, { modelId, category });
      this.touchLastUsed(modelId, model.sizeBytes ?? 0);
      return true;
    } catch (err) {
      const message = err instanceof Error ? err.message : typeof err === "object" ? JSON.stringify(err) : String(err);
      logger5.error(`Failed to load model ${modelId}: ${message}`);
      this.registry.updateModel(modelId, { status: ModelStatus.Error, error: message });
      EventBus.shared.emit("model.loadFailed", SDKEventType.Model, { modelId, error: message });
      return false;
    }
  }
  async unloadModel(modelId) {
    const model = this.registry.getModel(modelId);
    if (!model)
      return;
    const category = model.modality ?? ModelCategory.Language;
    await this.unloadModelByCategory(category);
  }
  async unloadAll(exceptModelId) {
    const loaded = [...this.loadedByCategory.entries()];
    if (loaded.length === 0)
      return;
    for (const [category, loadedId] of loaded) {
      if (exceptModelId && loadedId === exceptModelId)
        continue;
      logger5.info(`Unloading ${category} model (${loadedId}) — freeing resources`);
      await this.unloadModelByCategory(category);
    }
  }
  async deleteModel(modelId) {
    for (const [category, id] of this.loadedByCategory) {
      if (id === modelId) {
        this.loadedByCategory.delete(category);
        break;
      }
    }
    await this.downloader.deleteFromOPFS(modelId);
    const model = this.registry.getModel(modelId);
    if (model == null ? void 0 : model.additionalFiles) {
      for (const file of model.additionalFiles) {
        await this.downloader.deleteFromOPFS(this.downloader.additionalFileKey(modelId, file.filename));
      }
    }
    this.registry.updateModel(modelId, { status: ModelStatus.Registered, downloadProgress: void 0, sizeBytes: void 0 });
    this.removeMetadata(modelId);
  }
  async clearAll() {
    await this.storage.clearAll();
    this.metadata = {};
    this.loadedByCategory.clear();
    for (const model of this.registry.getModels()) {
      if (model.status !== ModelStatus.Registered) {
        this.registry.updateModel(model.id, {
          status: ModelStatus.Registered,
          downloadProgress: void 0,
          sizeBytes: void 0
        });
      }
    }
  }
  async getStorageInfo() {
    let modelCount = 0;
    let totalSize = 0;
    try {
      const root = await navigator.storage.getDirectory();
      const modelsDir = await root.getDirectoryHandle("models");
      for await (const [name, handle] of modelsDir.entries()) {
        if (handle.kind === "file" && !name.startsWith("_")) {
          modelCount++;
          const file = await handle.getFile();
          totalSize += file.size;
        }
      }
    } catch {
    }
    let available = 0;
    try {
      const estimate = await navigator.storage.estimate();
      available = (estimate.quota ?? 0) - (estimate.usage ?? 0);
    } catch {
    }
    return { modelCount, totalSize, available };
  }
  // --- LRU Metadata ---
  getModelLastUsedAt(modelId) {
    var _a;
    return ((_a = this.metadata[modelId]) == null ? void 0 : _a.lastUsedAt) ?? 0;
  }
  touchLastUsed(modelId, sizeBytes) {
    this.metadata[modelId] = { lastUsedAt: Date.now(), sizeBytes };
    this.storage.saveMetadata(this.metadata).catch(() => {
    });
  }
  removeMetadata(modelId) {
    delete this.metadata[modelId];
    this.storage.saveMetadata(this.metadata).catch(() => {
    });
  }
  // --- Subscriptions ---
  onChange(callback) {
    return this.registry.onChange(callback);
  }
  // ---------------------------------------------------------------------------
  // Private — model loading by modality
  // ---------------------------------------------------------------------------
  /**
   * Build a ModelLoadContext for passing to backend loaders.
   */
  buildLoadContext(model, data, dataStream, file) {
    return {
      model,
      data,
      dataStream,
      file,
      downloadFile: (url) => this.downloader.downloadFile(url),
      loadFile: (fileKey) => this.downloader.loadFromOPFS(fileKey),
      storeFile: (fileKey, fileData) => this.downloader.storeInOPFS(fileKey, fileData),
      additionalFileKey: (modelId, filename) => this.downloader.additionalFileKey(modelId, filename)
    };
  }
  /**
   * Load an LLM model via the pluggable loader.
   * The loader (in @runanywhere/web-llamacpp) handles writing to its own
   * Emscripten FS and calling the C API.
   */
  async loadLLMModel(model, _modelId, data, dataStream, file) {
    if (!this.llmLoader)
      throw new Error("No LLM loader registered. Register the @runanywhere/web-llamacpp package.");
    const ctx = this.buildLoadContext(model, data, dataStream, file);
    await this.llmLoader.loadModelFromData(ctx);
    logger5.info(`LLM model loaded: ${model.id}`);
  }
  /**
   * Load a VLM (vision-language) model via the pluggable VLM loader.
   */
  async loadVLMModel(model, modelId) {
    var _a;
    const exists = await this.downloader.existsInOPFS(modelId);
    if (!exists) {
      throw new Error("Model not downloaded — please download the model first.");
    }
    const mmprojFile = (_a = model.additionalFiles) == null ? void 0 : _a.find((f) => f.filename.includes("mmproj"));
    if (!mmprojFile) {
      logger5.warning(`No mmproj found, loading as text-only LLM: ${modelId}`);
      const file = await this.downloader.loadModelFile(modelId);
      let dataStream;
      let data;
      if (!file) {
        dataStream = await this.downloader.loadStreamFromOPFS(modelId) ?? void 0;
        if (!dataStream)
          data = await this.downloader.loadFromOPFS(modelId) ?? void 0;
      }
      if (!file && !dataStream && !data)
        throw new Error("Model not downloaded.");
      await this.loadLLMModel(model, modelId, data, dataStream, file ?? void 0);
      return;
    }
    const mmprojKey = this.downloader.additionalFileKey(modelId, mmprojFile.filename);
    const mmprojExists = await this.downloader.existsInOPFS(mmprojKey);
    if (!mmprojExists && mmprojFile.url) {
      logger5.debug(`mmproj not in storage, downloading on-demand: ${mmprojFile.filename}`);
      const mmprojDownload = await this.downloader.downloadFile(mmprojFile.url);
      await this.downloader.storeInOPFS(mmprojKey, mmprojDownload);
    }
    if (!this.vlmLoader) {
      throw new Error("No VLM loader registered. Call ModelManager.setVLMLoader() first.");
    }
    if (!this.vlmLoader.isInitialized) {
      logger5.info("Initializing VLM loader...");
      await this.vlmLoader.init();
    }
    let modelDataBuf;
    let mmprojDataBuf;
    const modelInOPFS = await this.downloader.existsInActualOPFS(modelId);
    if (!modelInOPFS) {
      const data = await this.downloader.loadFromOPFS(modelId);
      if (data && data.length > 0) {
        modelDataBuf = new ArrayBuffer(data.byteLength);
        new Uint8Array(modelDataBuf).set(data);
        logger5.debug(`Transferring model data to VLM Worker (${(data.length / 1024 / 1024).toFixed(1)} MB)`);
      }
    }
    const mmprojInOPFS = await this.downloader.existsInActualOPFS(mmprojKey);
    if (!mmprojInOPFS) {
      const mmprojBytes = await this.downloader.loadFromOPFS(mmprojKey);
      if (mmprojBytes) {
        mmprojDataBuf = new ArrayBuffer(mmprojBytes.byteLength);
        new Uint8Array(mmprojDataBuf).set(mmprojBytes);
        logger5.debug(`Transferring mmproj data to VLM Worker (${(mmprojBytes.length / 1024 / 1024).toFixed(1)} MB)`);
      }
    }
    logger5.info(`Loading VLM model: ${modelId}`);
    await this.vlmLoader.loadModel({
      modelOpfsKey: modelId,
      modelFilename: `${modelId}.gguf`,
      mmprojOpfsKey: mmprojKey,
      mmprojFilename: mmprojFile.filename,
      modelId,
      modelName: model.name,
      modelData: modelDataBuf,
      mmprojData: mmprojDataBuf
    });
    logger5.info(`VLM model loaded: ${modelId}`);
  }
  /**
   * Load an STT model via the pluggable loader.
   * All sherpa-onnx FS operations are handled by the loader.
   */
  async loadSTTModel(model, data) {
    if (!this.sttLoader)
      throw new Error("No STT loader registered. Register the @runanywhere/web-onnx package.");
    const ctx = this.buildLoadContext(model, data);
    await this.sttLoader.loadModelFromData(ctx);
    logger5.info(`STT model loaded: ${model.id}`);
  }
  /**
   * Load a TTS model via the pluggable loader.
   * All sherpa-onnx FS operations are handled by the loader.
   */
  async loadTTSModel(model, data) {
    if (!this.ttsLoader)
      throw new Error("No TTS loader registered. Register the @runanywhere/web-onnx package.");
    const ctx = this.buildLoadContext(model, data);
    await this.ttsLoader.loadModelFromData(ctx);
    logger5.info(`TTS model loaded: ${model.id}`);
  }
  /**
   * Load a VAD model via the pluggable loader.
   * All sherpa-onnx FS operations are handled by the loader.
   */
  async loadVADModel(model, data) {
    if (!this.vadLoader)
      throw new Error("No VAD loader registered. Register the @runanywhere/web-onnx package.");
    const ctx = this.buildLoadContext(model, data);
    await this.vadLoader.loadModelFromData(ctx);
    logger5.info(`VAD model loaded: ${model.id}`);
  }
  /** Unload the currently loaded model for a specific category */
  async unloadModelByCategory(category) {
    var _a, _b, _c, _d, _e, _f;
    const modelId = this.loadedByCategory.get(category);
    if (!modelId)
      return;
    logger5.info(`Unloading ${category} model: ${modelId}`);
    try {
      if (category === ModelCategory.SpeechRecognition) {
        await ((_a = this.sttLoader) == null ? void 0 : _a.unloadModel());
      } else if (category === ModelCategory.SpeechSynthesis) {
        await ((_b = this.ttsLoader) == null ? void 0 : _b.unloadVoice());
      } else if (category === ModelCategory.Audio) {
        (_c = this.vadLoader) == null ? void 0 : _c.cleanup();
      } else if (category === ModelCategory.Multimodal) {
        await ((_d = this.vlmLoader) == null ? void 0 : _d.unloadModel());
      } else {
        if ((_e = this.llmLoader) == null ? void 0 : _e.unloadAndCleanup) {
          await this.llmLoader.unloadAndCleanup(modelId);
        } else {
          await ((_f = this.llmLoader) == null ? void 0 : _f.unloadModel());
        }
      }
    } catch (err) {
      logger5.warning(`Error during unload of ${modelId}: ${err instanceof Error ? err.message : String(err)}`);
    }
    this.registry.updateModel(modelId, { status: ModelStatus.Downloaded });
    this.loadedByCategory.delete(category);
    EventBus.shared.emit("model.unloaded", SDKEventType.Model, { modelId, category });
  }
};
var ModelManager = new ModelManagerImpl();

// node_modules/@runanywhere/web/dist/Infrastructure/ExtensionRegistry.js
var ExtensionRegistryImpl = class {
  constructor() {
    __publicField(this, "extensions", []);
  }
  /** Register an extension. Call in dependency order (low-level first). */
  register(extension) {
    if (!this.extensions.includes(extension)) {
      this.extensions.push(extension);
    }
  }
  /**
   * Cleanup all registered extensions in reverse registration order.
   * Each cleanup is individually wrapped in try-catch so one failure
   * does not prevent the rest from being cleaned up.
   */
  cleanupAll() {
    for (let i = this.extensions.length - 1; i >= 0; i--) {
      try {
        this.extensions[i].cleanup();
      } catch {
      }
    }
  }
  /** Reset the registry (call after full shutdown). */
  reset() {
    this.extensions = [];
  }
};
var ExtensionRegistry = new ExtensionRegistryImpl();

// node_modules/@runanywhere/web/dist/Infrastructure/ExtensionPoint.js
var logger6 = new SDKLogger("ExtensionPoint");
var BackendCapability;
(function(BackendCapability2) {
  BackendCapability2["LLM"] = "llm";
  BackendCapability2["VLM"] = "vlm";
  BackendCapability2["STT"] = "stt";
  BackendCapability2["TTS"] = "tts";
  BackendCapability2["VAD"] = "vad";
  BackendCapability2["Embeddings"] = "embeddings";
  BackendCapability2["Diffusion"] = "diffusion";
  BackendCapability2["ToolCalling"] = "toolCalling";
  BackendCapability2["StructuredOutput"] = "structuredOutput";
})(BackendCapability || (BackendCapability = {}));
var ServiceKey;
(function(ServiceKey2) {
  ServiceKey2["TextGeneration"] = "textGeneration";
  ServiceKey2["STT"] = "stt";
  ServiceKey2["TTS"] = "tts";
  ServiceKey2["VLM"] = "vlm";
  ServiceKey2["Embeddings"] = "embeddings";
  ServiceKey2["Diffusion"] = "diffusion";
  ServiceKey2["ToolCalling"] = "toolCalling";
  ServiceKey2["VAD"] = "vad";
})(ServiceKey || (ServiceKey = {}));
var ExtensionPointImpl = class {
  constructor() {
    __publicField(this, "backends", /* @__PURE__ */ new Map());
    __publicField(this, "capabilityMap", /* @__PURE__ */ new Map());
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    __publicField(this, "services", /* @__PURE__ */ new Map());
    // -------------------------------------------------------------------------
    // Provider Registry — typed cross-package provider access (issue #371)
    // -------------------------------------------------------------------------
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    __publicField(this, "providers", /* @__PURE__ */ new Map());
  }
  /**
   * Register a backend extension.
   * Idempotent — re-registering the same id is a no-op.
   */
  registerBackend(extension) {
    if (this.backends.has(extension.id)) {
      logger6.debug(`Backend '${extension.id}' already registered, skipping`);
      return;
    }
    this.backends.set(extension.id, extension);
    for (const cap of extension.capabilities) {
      if (this.capabilityMap.has(cap)) {
        logger6.warning(`Capability '${cap}' already provided by '${this.capabilityMap.get(cap).id}', overriding with '${extension.id}'`);
      }
      this.capabilityMap.set(cap, extension);
    }
    logger6.info(`Backend '${extension.id}' registered — capabilities: [${extension.capabilities.join(", ")}]`);
  }
  /** Get a backend by its id. */
  getBackend(id) {
    return this.backends.get(id);
  }
  /** Check if a capability is available (i.e. a backend providing it is registered). */
  hasCapability(capability) {
    return this.capabilityMap.has(capability);
  }
  /** Get the backend extension providing a given capability. */
  getExtensionForCapability(capability) {
    return this.capabilityMap.get(capability);
  }
  /**
   * Require that a capability is available. Throws a clear error if not.
   * Use in extension methods that depend on a backend being registered.
   */
  requireCapability(capability) {
    if (!this.capabilityMap.has(capability)) {
      const packageHint = capability === BackendCapability.LLM || capability === BackendCapability.VLM || capability === BackendCapability.Embeddings || capability === BackendCapability.Diffusion || capability === BackendCapability.ToolCalling || capability === BackendCapability.StructuredOutput ? "@runanywhere/web-llamacpp" : "@runanywhere/web-onnx";
      throw new Error(`Capability '${capability}' not available. Install and register the ${packageHint} package.`);
    }
  }
  /**
   * Register a typed provider implementation for a capability.
   *
   * Backend packages call this during their registration phase:
   * ```ts
   * ExtensionPoint.registerProvider('llm', TextGeneration);
   * ExtensionPoint.registerProvider('stt', STT);
   * ```
   */
  registerProvider(capability, implementation) {
    if (this.providers.has(capability)) {
      logger6.debug(`Provider '${capability}' already registered, overwriting`);
    }
    this.providers.set(capability, implementation);
    logger6.debug(`Provider '${capability}' registered`);
  }
  /**
   * Retrieve a registered provider by capability.
   * Returns undefined if no provider is registered for the given capability.
   */
  getProvider(capability) {
    return this.providers.get(capability);
  }
  /**
   * Retrieve a registered provider or throw a descriptive error.
   * Use in code that requires a specific backend to be registered.
   */
  requireProvider(capability, packageHint) {
    const provider = this.providers.get(capability);
    if (!provider) {
      const hint = packageHint ?? (capability === "stt" || capability === "tts" ? "@runanywhere/web-onnx" : "@runanywhere/web-llamacpp");
      throw new Error(`Provider '${capability}' not available. Install and register the ${hint} package.`);
    }
    return provider;
  }
  /** Remove a registered provider. */
  removeProvider(capability) {
    this.providers.delete(capability);
  }
  // -------------------------------------------------------------------------
  // Service Registry — typed singleton access for cross-package communication
  // -------------------------------------------------------------------------
  /**
   * Register a service singleton under a typed key.
   * Backend packages call this during their registration phase.
   */
  registerService(key, service) {
    if (this.services.has(key)) {
      logger6.debug(`Service '${key}' already registered, overwriting`);
    }
    this.services.set(key, service);
  }
  /**
   * Retrieve a registered service singleton.
   * Returns undefined if the service is not registered yet.
   */
  getService(key) {
    return this.services.get(key);
  }
  /**
   * Retrieve a registered service or throw a descriptive error.
   * Use in code that requires a specific backend to be registered.
   */
  requireService(key, packageHint) {
    const service = this.services.get(key);
    if (!service) {
      const hint = packageHint ?? (key === ServiceKey.STT || key === ServiceKey.TTS || key === ServiceKey.VAD ? "@runanywhere/web-onnx" : "@runanywhere/web-llamacpp");
      throw new Error(`Service '${key}' not available. Install and register the ${hint} package.`);
    }
    return service;
  }
  /** Remove a registered service. */
  removeService(key) {
    this.services.delete(key);
  }
  /**
   * Cleanup all registered backends in reverse registration order.
   * Called during SDK shutdown.
   */
  cleanupAll() {
    const entries = [...this.backends.entries()].reverse();
    for (const [id, backend] of entries) {
      try {
        backend.cleanup();
        logger6.debug(`Backend '${id}' cleaned up`);
      } catch {
      }
    }
  }
  /** Reset the registry (call after full shutdown). */
  reset() {
    this.backends.clear();
    this.capabilityMap.clear();
    this.services.clear();
    this.providers.clear();
  }
};
var ExtensionPoint = new ExtensionPointImpl();

// node_modules/@runanywhere/web/dist/Infrastructure/LocalFileStorage.js
var logger7 = new SDKLogger("LocalFileStorage");
var DB_NAME = "runanywhere-storage";
var DB_VERSION = 1;
var STORE_NAME = "handles";
var HANDLE_KEY = "modelDirectory";
var LS_DIR_NAME_KEY = "runanywhere_storage_dir_name";
var LocalFileStorage = class _LocalFileStorage {
  constructor() {
    __publicField(this, "dirHandle", null);
    __publicField(this, "_isReady", false);
    __publicField(this, "_hasStoredHandle", false);
    /** Per-key write lock to prevent concurrent writes to the same file. */
    __publicField(this, "writeLocks", /* @__PURE__ */ new Map());
  }
  // -------------------------------------------------------------------------
  // Static
  // -------------------------------------------------------------------------
  /** Whether the File System Access API is available in this browser. */
  static get isSupported() {
    return typeof window !== "undefined" && "showDirectoryPicker" in window;
  }
  /**
   * Get the stored directory name from localStorage (fast, synchronous).
   * Available immediately on page load before IndexedDB restores the handle.
   * Returns the folder name only (e.g. "ai-models"), not the full path.
   */
  static get storedDirectoryName() {
    try {
      return localStorage.getItem(LS_DIR_NAME_KEY);
    } catch {
      return null;
    }
  }
  // -------------------------------------------------------------------------
  // State
  // -------------------------------------------------------------------------
  /** Whether the storage is ready for use (directory selected + permission granted). */
  get isReady() {
    return this._isReady && this.dirHandle !== null;
  }
  /** Whether a handle was found in IndexedDB (even if permission isn't granted yet). */
  get hasStoredHandle() {
    return this._hasStoredHandle;
  }
  /** The name of the chosen directory (for display in UI). */
  get directoryName() {
    var _a;
    return ((_a = this.dirHandle) == null ? void 0 : _a.name) ?? null;
  }
  // -------------------------------------------------------------------------
  // Directory Selection
  // -------------------------------------------------------------------------
  /**
   * Prompt the user to choose a directory for model storage.
   * Opens the OS folder picker dialog.
   * Stores the directory handle in IndexedDB for future sessions.
   *
   * @returns true if a directory was selected, false if cancelled
   */
  async chooseDirectory() {
    if (!_LocalFileStorage.isSupported) {
      logger7.warning("File System Access API not supported in this browser");
      return false;
    }
    try {
      const win = window;
      this.dirHandle = await win.showDirectoryPicker({
        mode: "readwrite"
      });
      await this.storeHandle(this.dirHandle);
      this._isReady = true;
      this._hasStoredHandle = true;
      try {
        localStorage.setItem(LS_DIR_NAME_KEY, this.dirHandle.name);
      } catch {
      }
      logger7.info(`Local storage directory selected: ${this.dirHandle.name}`);
      return true;
    } catch (err) {
      if (err instanceof Error && err.name === "AbortError") {
        logger7.debug("User cancelled directory picker");
        return false;
      }
      const message = err instanceof Error ? err.message : String(err);
      logger7.error(`Failed to choose directory: ${message}`);
      return false;
    }
  }
  /**
   * Attempt to restore a previously chosen directory from IndexedDB.
   *
   * On Chrome 122+, if the user selected "Allow on every visit",
   * permission is automatically granted without any prompt.
   *
   * @returns true if directory was restored and permission is granted.
   *          false if no handle stored or permission not granted (UI
   *          should show a "Re-authorize" button).
   */
  async restoreDirectory() {
    if (!_LocalFileStorage.isSupported)
      return false;
    try {
      const handle = await this.retrieveHandle();
      if (!handle) {
        logger7.debug("No stored directory handle found");
        return false;
      }
      this._hasStoredHandle = true;
      this.dirHandle = handle;
      const permission = await handle.queryPermission({ mode: "readwrite" });
      if (permission === "granted") {
        this._isReady = true;
        logger7.info(`Local storage restored: ${handle.name}`);
        return true;
      }
      logger7.debug(`Local storage handle found but permission is '${permission}' — needs re-authorization`);
      this._isReady = false;
      return false;
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      logger7.warning(`Failed to restore directory: ${message}`);
      return false;
    }
  }
  /**
   * Request readwrite permission on a previously stored handle.
   * MUST be called from a user gesture (button click handler).
   *
   * @returns true if permission was granted
   */
  async requestAccess() {
    if (!this.dirHandle) {
      logger7.warning("No directory handle to request access for");
      return false;
    }
    try {
      const permission = await this.dirHandle.requestPermission({ mode: "readwrite" });
      if (permission === "granted") {
        this._isReady = true;
        logger7.info(`Local storage re-authorized: ${this.dirHandle.name}`);
        return true;
      }
      logger7.debug(`Permission request result: ${permission}`);
      return false;
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      logger7.warning(`Permission request failed: ${message}`);
      return false;
    }
  }
  // -------------------------------------------------------------------------
  // Model Operations
  // -------------------------------------------------------------------------
  /**
   * Save model data to the local filesystem.
   * Uses a per-key lock to prevent concurrent writes from corrupting files.
   * @param key - Model identifier (used as filename)
   * @param data - Model file data
   */
  async saveModel(key, data) {
    return this.withWriteLock(key, () => this._saveModelImpl(key, data));
  }
  /**
   * Save model data from a ReadableStream to the local filesystem.
   * Streams data directly to disk without buffering the entire file in memory.
   * Uses a per-key lock to prevent concurrent writes from corrupting files.
   * @param key - Model identifier (used as filename)
   * @param stream - Readable stream of model data
   */
  async saveModelFromStream(key, stream) {
    return this.withWriteLock(key, () => this._saveStreamImpl(key, stream));
  }
  async _saveModelImpl(key, data) {
    if (!this.dirHandle || !this._isReady) {
      throw new Error("LocalFileStorage not ready — call chooseDirectory() or restoreDirectory() first.");
    }
    const filename = this.sanitizeFilename(key);
    const fileHandle = await this.dirHandle.getFileHandle(filename, { create: true });
    const writable = await fileHandle.createWritable();
    try {
      await writable.write(data);
      await writable.close();
      logger7.info(`Saved model to local storage: ${filename} (${(data.byteLength / 1024 / 1024).toFixed(1)} MB)`);
    } catch (err) {
      try {
        await writable.abort();
      } catch {
      }
      throw err;
    }
  }
  async _saveStreamImpl(key, stream) {
    if (!this.dirHandle || !this._isReady) {
      throw new Error("LocalFileStorage not ready — call chooseDirectory() or restoreDirectory() first.");
    }
    const filename = this.sanitizeFilename(key);
    const fileHandle = await this.dirHandle.getFileHandle(filename, { create: true });
    const writable = await fileHandle.createWritable();
    try {
      const reader = stream.getReader();
      while (true) {
        const { done, value } = await reader.read();
        if (done)
          break;
        await writable.write(value);
      }
      await writable.close();
      logger7.info(`Streamed model to local storage: ${filename}`);
    } catch (err) {
      try {
        await writable.abort();
      } catch {
      }
      throw err;
    }
  }
  /**
   * Per-key write lock: ensures only one write operation per key at a time.
   * Concurrent calls for the same key will be serialized.
   */
  async withWriteLock(key, fn) {
    const prev = this.writeLocks.get(key) ?? Promise.resolve();
    const next = prev.then(fn, fn);
    this.writeLocks.set(key, next);
    try {
      await next;
    } finally {
      if (this.writeLocks.get(key) === next) {
        this.writeLocks.delete(key);
      }
    }
  }
  /**
   * Load model data from the local filesystem.
   * @param key - Model identifier
   * @returns Model data, or null if not found
   */
  async loadModel(key) {
    if (!this.dirHandle || !this._isReady)
      return null;
    try {
      const filename = this.sanitizeFilename(key);
      const fileHandle = await this.dirHandle.getFileHandle(filename);
      const file = await fileHandle.getFile();
      logger7.info(`Loaded model from local storage: ${filename} (${(file.size / 1024 / 1024).toFixed(1)} MB)`);
      return await file.arrayBuffer();
    } catch {
      return null;
    }
  }
  /**
   * Load model data from the local filesystem as a ReadableStream.
   * @param key - Model identifier
   * @returns Readable stream of the model data, or null if not found
   */
  async loadModelStream(key) {
    if (!this.dirHandle || !this._isReady)
      return null;
    try {
      const filename = this.sanitizeFilename(key);
      const fileHandle = await this.dirHandle.getFileHandle(filename);
      const file = await fileHandle.getFile();
      logger7.info(`Loading model stream from local storage: ${filename} (${(file.size / 1024 / 1024).toFixed(1)} MB)`);
      return file.stream();
    } catch {
      return null;
    }
  }
  /**
   * Get the File object for a model without reading into memory.
   * Enables streaming / mounting for locally stored files.
   * @param key - Model identifier
   */
  async loadModelFile(key) {
    if (!this.dirHandle || !this._isReady)
      return null;
    try {
      const filename = this.sanitizeFilename(key);
      const fileHandle = await this.dirHandle.getFileHandle(filename);
      return await fileHandle.getFile();
    } catch {
      return null;
    }
  }
  /**
   * Check if a model file exists in local storage.
   * @param key - Model identifier
   */
  async hasModel(key) {
    if (!this.dirHandle || !this._isReady)
      return false;
    try {
      const filename = this.sanitizeFilename(key);
      await this.dirHandle.getFileHandle(filename);
      return true;
    } catch {
      return false;
    }
  }
  /**
   * Delete a model file from local storage.
   * @param key - Model identifier
   */
  async deleteModel(key) {
    if (!this.dirHandle || !this._isReady)
      return;
    try {
      const filename = this.sanitizeFilename(key);
      await this.dirHandle.removeEntry(filename);
      logger7.info(`Deleted model from local storage: ${filename}`);
    } catch {
    }
  }
  /**
   * Get file size without reading into memory.
   * @param key - Model identifier
   */
  async getFileSize(key) {
    if (!this.dirHandle || !this._isReady)
      return null;
    try {
      const filename = this.sanitizeFilename(key);
      const fileHandle = await this.dirHandle.getFileHandle(filename);
      const file = await fileHandle.getFile();
      return file.size;
    } catch {
      return null;
    }
  }
  /**
   * List all model files in the directory.
   */
  async listModels() {
    if (!this.dirHandle || !this._isReady)
      return [];
    const models = [];
    for await (const [name, handle] of this.dirHandle.entries()) {
      if (handle.kind === "file") {
        const file = await handle.getFile();
        models.push({
          id: name,
          sizeBytes: file.size,
          lastModified: file.lastModified
        });
      }
    }
    return models;
  }
  // -------------------------------------------------------------------------
  // IndexedDB Handle Persistence
  // -------------------------------------------------------------------------
  async openDB() {
    return new Promise((resolve, reject) => {
      const request = indexedDB.open(DB_NAME, DB_VERSION);
      request.onupgradeneeded = () => {
        const db = request.result;
        if (!db.objectStoreNames.contains(STORE_NAME)) {
          db.createObjectStore(STORE_NAME);
        }
      };
      request.onsuccess = () => resolve(request.result);
      request.onerror = () => reject(request.error);
    });
  }
  async storeHandle(handle) {
    try {
      const db = await this.openDB();
      const tx = db.transaction(STORE_NAME, "readwrite");
      tx.objectStore(STORE_NAME).put(handle, HANDLE_KEY);
      await new Promise((resolve, reject) => {
        tx.oncomplete = () => resolve();
        tx.onerror = () => reject(tx.error);
      });
      db.close();
      logger7.debug("Directory handle stored in IndexedDB");
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      logger7.warning(`Failed to store handle in IndexedDB: ${message}`);
    }
  }
  async retrieveHandle() {
    try {
      const db = await this.openDB();
      const tx = db.transaction(STORE_NAME, "readonly");
      const handle = await new Promise((resolve, reject) => {
        const req = tx.objectStore(STORE_NAME).get(HANDLE_KEY);
        req.onsuccess = () => resolve(req.result ?? null);
        req.onerror = () => reject(req.error);
      });
      db.close();
      return handle;
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      logger7.warning(`Failed to retrieve handle from IndexedDB: ${message}`);
      return null;
    }
  }
  // -------------------------------------------------------------------------
  // Helpers
  // -------------------------------------------------------------------------
  /**
   * Sanitize a key for use as a filename.
   * Keeps alphanumeric, dots, dashes, underscores. Replaces everything else.
   */
  sanitizeFilename(key) {
    return key.replace(/[<>:"/\\|?*\x00-\x1F]/g, "_");
  }
};

// node_modules/@runanywhere/web/dist/Public/RunAnywhere.js
var logger8 = new SDKLogger("RunAnywhere");
var _isInitialized = false;
var _initOptions = null;
var _initializingPromise = null;
var _localFileStorage = null;
var RunAnywhere = {
  // =========================================================================
  // SDK State
  // =========================================================================
  get isInitialized() {
    return _isInitialized;
  },
  get version() {
    return "0.1.0";
  },
  get environment() {
    return (_initOptions == null ? void 0 : _initOptions.environment) ?? null;
  },
  get events() {
    return EventBus.shared;
  },
  // =========================================================================
  // Initialization (pure TypeScript — no WASM)
  // =========================================================================
  /**
   * Initialize the RunAnywhere SDK.
   *
   * This only initializes the TypeScript infrastructure:
   *   1. Configure logging
   *   2. Initialize storage (OPFS)
   *   3. Restore local file storage (if previously configured)
   *
   * WASM is loaded lazily by each backend package when you call:
   *   await LlamaCPP.register();  // loads racommons-llamacpp.wasm
   *   await ONNX.register();      // loads sherpa-onnx.wasm (on first use)
   */
  async initialize(options = {}) {
    if (_isInitialized) {
      logger8.debug("Already initialized");
      return;
    }
    if (_initializingPromise) {
      logger8.debug("Initialization already in progress, awaiting...");
      return _initializingPromise;
    }
    _initializingPromise = (async () => {
      try {
        const env = options.environment ?? SDKEnvironment.Development;
        _initOptions = { ...options, environment: env };
        if (options.debug) {
          SDKLogger.level = LogLevel.Debug;
        }
        logger8.info(`Initializing RunAnywhere Web SDK (${env})...`);
        try {
          await RunAnywhere.restoreLocalStorage();
        } catch (err) {
          logger8.warning(`Failed to restore local storage: ${err instanceof Error ? err.message : String(err)}`);
        }
        _isInitialized = true;
        logger8.info("RunAnywhere Web SDK initialized successfully");
        EventBus.shared.emit("sdk.initialized", SDKEventType.Initialization, {
          environment: env
        });
      } finally {
        _initializingPromise = null;
      }
    })();
    return _initializingPromise;
  },
  // =========================================================================
  // Model Management
  // =========================================================================
  registerModels(models) {
    ModelManager.registerModels(models);
  },
  setVLMLoader(loader) {
    ModelManager.setVLMLoader(loader);
  },
  async downloadModel(modelId) {
    return ModelManager.downloadModel(modelId);
  },
  async loadModel(modelId) {
    return ModelManager.loadModel(modelId);
  },
  availableModels() {
    return ModelManager.getModels();
  },
  getLoadedModel(category) {
    return ModelManager.getLoadedModel(category);
  },
  async unloadAll() {
    return ModelManager.unloadAll();
  },
  async deleteModel(modelId) {
    return ModelManager.deleteModel(modelId);
  },
  // =========================================================================
  // Model Import (file picker / drag-and-drop)
  // =========================================================================
  async importModelFromPicker(options) {
    const acceptExts = (options == null ? void 0 : options.accept) ?? [".gguf", ".onnx", ".bin"];
    if ("showOpenFilePicker" in window) {
      try {
        const [handle] = await window.showOpenFilePicker({
          types: [{
            description: "AI Model Files",
            accept: { "application/octet-stream": acceptExts }
          }],
          multiple: false
        });
        const file = await handle.getFile();
        return this.importModelFromFile(file, options);
      } catch (err) {
        if (err instanceof Error && err.name === "AbortError")
          return null;
        logger8.debug("showOpenFilePicker failed, using input fallback");
      }
    }
    return new Promise((resolve) => {
      const input = document.createElement("input");
      input.type = "file";
      input.accept = acceptExts.join(",");
      input.style.display = "none";
      let settled = false;
      const cleanup = () => {
        if (input.parentNode) {
          document.body.removeChild(input);
        }
      };
      const settle = (value) => {
        if (settled)
          return;
        settled = true;
        cleanup();
        resolve(value);
      };
      input.onchange = async () => {
        var _a;
        const file = (_a = input.files) == null ? void 0 : _a[0];
        if (!file) {
          settle(null);
          return;
        }
        try {
          const id = await this.importModelFromFile(file, options);
          settle(id);
        } catch (err) {
          logger8.error(`Import failed: ${err instanceof Error ? err.message : String(err)}`);
          settle(null);
        }
      };
      input.addEventListener("cancel", () => settle(null));
      const fallbackCleanup = () => {
        setTimeout(() => {
          if (!settled) {
            settle(null);
          }
        }, 300);
        window.removeEventListener("focus", fallbackCleanup);
        document.removeEventListener("visibilitychange", fallbackCleanup);
      };
      window.addEventListener("focus", fallbackCleanup);
      document.addEventListener("visibilitychange", fallbackCleanup);
      document.body.appendChild(input);
      input.click();
    });
  },
  async importModelFromFile(file, options) {
    logger8.info(`Importing model from file: ${file.name} (${(file.size / 1024 / 1024).toFixed(1)} MB)`);
    return ModelManager.importModel(file, options == null ? void 0 : options.modelId);
  },
  // =========================================================================
  // Local File Storage (persistent model storage)
  // =========================================================================
  get isLocalStorageSupported() {
    return LocalFileStorage.isSupported;
  },
  get isLocalStorageReady() {
    return (_localFileStorage == null ? void 0 : _localFileStorage.isReady) ?? false;
  },
  get hasLocalStorageHandle() {
    return (_localFileStorage == null ? void 0 : _localFileStorage.hasStoredHandle) ?? false;
  },
  get localStorageDirectoryName() {
    return (_localFileStorage == null ? void 0 : _localFileStorage.directoryName) ?? LocalFileStorage.storedDirectoryName;
  },
  async chooseLocalStorageDirectory() {
    if (!LocalFileStorage.isSupported) {
      logger8.warning("File System Access API not supported — using browser storage (OPFS)");
      return false;
    }
    if (!_localFileStorage) {
      _localFileStorage = new LocalFileStorage();
    }
    const success = await _localFileStorage.chooseDirectory();
    if (success) {
      ModelManager.setLocalFileStorage(_localFileStorage);
      EventBus.shared.emit("storage.localDirectorySelected", SDKEventType.Storage, {
        directoryName: _localFileStorage.directoryName
      });
    }
    return success;
  },
  async restoreLocalStorage() {
    if (!LocalFileStorage.isSupported)
      return false;
    if (!_localFileStorage) {
      _localFileStorage = new LocalFileStorage();
    }
    const success = await _localFileStorage.restoreDirectory();
    if (success) {
      ModelManager.setLocalFileStorage(_localFileStorage);
      logger8.info(`Local storage restored: ${_localFileStorage.directoryName}`);
    }
    return success;
  },
  async requestLocalStorageAccess() {
    if (!_localFileStorage)
      return false;
    const success = await _localFileStorage.requestAccess();
    if (success) {
      ModelManager.setLocalFileStorage(_localFileStorage);
    }
    return success;
  },
  // =========================================================================
  // Shutdown
  // =========================================================================
  shutdown() {
    logger8.info("Shutting down RunAnywhere Web SDK...");
    ModelManager.unloadAll().catch(() => {
    });
    ExtensionRegistry.cleanupAll();
    ExtensionPoint.cleanupAll();
    EventBus.reset();
    ExtensionRegistry.reset();
    ExtensionPoint.reset();
    _isInitialized = false;
    _initOptions = null;
    _initializingPromise = null;
    _localFileStorage = null;
    logger8.info("RunAnywhere Web SDK shut down");
  },
  reset() {
    RunAnywhere.shutdown();
  }
};

// node_modules/@runanywhere/web/dist/Foundation/ErrorTypes.js
var SDKErrorCode;
(function(SDKErrorCode2) {
  SDKErrorCode2[SDKErrorCode2["Success"] = 0] = "Success";
  SDKErrorCode2[SDKErrorCode2["NotInitialized"] = -100] = "NotInitialized";
  SDKErrorCode2[SDKErrorCode2["AlreadyInitialized"] = -101] = "AlreadyInitialized";
  SDKErrorCode2[SDKErrorCode2["InvalidConfiguration"] = -102] = "InvalidConfiguration";
  SDKErrorCode2[SDKErrorCode2["InitializationFailed"] = -103] = "InitializationFailed";
  SDKErrorCode2[SDKErrorCode2["ModelNotFound"] = -110] = "ModelNotFound";
  SDKErrorCode2[SDKErrorCode2["ModelLoadFailed"] = -111] = "ModelLoadFailed";
  SDKErrorCode2[SDKErrorCode2["ModelInvalidFormat"] = -112] = "ModelInvalidFormat";
  SDKErrorCode2[SDKErrorCode2["ModelNotLoaded"] = -113] = "ModelNotLoaded";
  SDKErrorCode2[SDKErrorCode2["ModelAlreadyLoaded"] = -114] = "ModelAlreadyLoaded";
  SDKErrorCode2[SDKErrorCode2["GenerationFailed"] = -130] = "GenerationFailed";
  SDKErrorCode2[SDKErrorCode2["GenerationCancelled"] = -131] = "GenerationCancelled";
  SDKErrorCode2[SDKErrorCode2["GenerationTimeout"] = -132] = "GenerationTimeout";
  SDKErrorCode2[SDKErrorCode2["InvalidPrompt"] = -133] = "InvalidPrompt";
  SDKErrorCode2[SDKErrorCode2["ContextLengthExceeded"] = -134] = "ContextLengthExceeded";
  SDKErrorCode2[SDKErrorCode2["NetworkError"] = -150] = "NetworkError";
  SDKErrorCode2[SDKErrorCode2["NetworkTimeout"] = -151] = "NetworkTimeout";
  SDKErrorCode2[SDKErrorCode2["AuthenticationFailed"] = -152] = "AuthenticationFailed";
  SDKErrorCode2[SDKErrorCode2["DownloadFailed"] = -160] = "DownloadFailed";
  SDKErrorCode2[SDKErrorCode2["DownloadCancelled"] = -161] = "DownloadCancelled";
  SDKErrorCode2[SDKErrorCode2["StorageError"] = -180] = "StorageError";
  SDKErrorCode2[SDKErrorCode2["InsufficientStorage"] = -181] = "InsufficientStorage";
  SDKErrorCode2[SDKErrorCode2["FileNotFound"] = -182] = "FileNotFound";
  SDKErrorCode2[SDKErrorCode2["FileWriteFailed"] = -183] = "FileWriteFailed";
  SDKErrorCode2[SDKErrorCode2["InvalidParameter"] = -220] = "InvalidParameter";
  SDKErrorCode2[SDKErrorCode2["ComponentNotReady"] = -230] = "ComponentNotReady";
  SDKErrorCode2[SDKErrorCode2["ComponentBusy"] = -231] = "ComponentBusy";
  SDKErrorCode2[SDKErrorCode2["InvalidState"] = -232] = "InvalidState";
  SDKErrorCode2[SDKErrorCode2["BackendNotAvailable"] = -600] = "BackendNotAvailable";
  SDKErrorCode2[SDKErrorCode2["BackendError"] = -601] = "BackendError";
  SDKErrorCode2[SDKErrorCode2["WASMLoadFailed"] = -900] = "WASMLoadFailed";
  SDKErrorCode2[SDKErrorCode2["WASMNotLoaded"] = -901] = "WASMNotLoaded";
  SDKErrorCode2[SDKErrorCode2["WASMCallbackError"] = -902] = "WASMCallbackError";
  SDKErrorCode2[SDKErrorCode2["WASMMemoryError"] = -903] = "WASMMemoryError";
})(SDKErrorCode || (SDKErrorCode = {}));
var SDKError = class _SDKError extends Error {
  constructor(code, message, details) {
    super(message);
    __publicField(this, "code");
    __publicField(this, "details");
    this.name = "SDKError";
    this.code = code;
    this.details = details;
  }
  /** Create from a RACommons rac_result_t error code */
  static fromRACResult(resultCode, details) {
    const message = `RACommons error: ${resultCode}`;
    return new _SDKError(resultCode, message, details);
  }
  /** Check if error code indicates success */
  static isSuccess(resultCode) {
    return resultCode === 0;
  }
  /** Convenience constructors */
  static notInitialized(message = "SDK not initialized") {
    return new _SDKError(SDKErrorCode.NotInitialized, message);
  }
  static wasmNotLoaded(message = "WASM module not loaded") {
    return new _SDKError(SDKErrorCode.WASMNotLoaded, message);
  }
  static modelNotFound(modelId) {
    return new _SDKError(SDKErrorCode.ModelNotFound, `Model not found: ${modelId}`);
  }
  static componentNotReady(component, details) {
    return new _SDKError(SDKErrorCode.ComponentNotReady, `Component not ready: ${component}`, details);
  }
  static generationFailed(details) {
    return new _SDKError(SDKErrorCode.GenerationFailed, "Generation failed", details);
  }
};
function isSDKError(error) {
  return error instanceof SDKError;
}

// node_modules/@runanywhere/web/dist/Public/Extensions/VoiceAgentTypes.js
var PipelineState;
(function(PipelineState2) {
  PipelineState2["Idle"] = "idle";
  PipelineState2["Listening"] = "listening";
  PipelineState2["ProcessingSTT"] = "processingSTT";
  PipelineState2["GeneratingResponse"] = "generatingResponse";
  PipelineState2["PlayingTTS"] = "playingTTS";
  PipelineState2["Cooldown"] = "cooldown";
  PipelineState2["Error"] = "error";
})(PipelineState || (PipelineState = {}));

// node_modules/@runanywhere/web/dist/Public/Extensions/RunAnywhere+VoiceAgent.js
var logger9 = new SDKLogger("VoiceAgent");
var VoiceAgentSession = class {
  constructor(handle) {
    __publicField(this, "_handle");
    this._handle = handle;
  }
  /**
   * Load models for all components.
   *
   * TODO: Delegate to backend provider via ExtensionPoint.
   */
  async loadModels(models) {
    if (models.stt)
      logger9.info(`Loading STT model: ${models.stt.id}`);
    if (models.llm)
      logger9.info(`Loading LLM model: ${models.llm.id}`);
    if (models.tts)
      logger9.info(`Loading TTS voice: ${models.tts.id}`);
    throw SDKError.componentNotReady("VoiceAgent", "No WASM backend registered — use a backend package (e.g. @runanywhere/web-llamacpp)");
  }
  /**
   * Process a complete voice turn (audio in → text response + audio out).
   *
   * TODO: Delegate to backend provider via ExtensionPoint.
   */
  async processVoiceTurn(_audioData) {
    throw SDKError.componentNotReady("VoiceAgent", "No WASM backend registered — use a backend package (e.g. @runanywhere/web-llamacpp)");
  }
  /**
   * Check if the voice agent is ready.
   *
   * TODO: Delegate to backend provider via ExtensionPoint.
   */
  get isReady() {
    return false;
  }
  /**
   * Transcribe audio without the full pipeline.
   *
   * TODO: Delegate to backend provider via ExtensionPoint.
   */
  async transcribe(_audioData) {
    throw SDKError.componentNotReady("VoiceAgent", "No WASM backend registered — use a backend package (e.g. @runanywhere/web-llamacpp)");
  }
  /**
   * Generate LLM response without the full pipeline.
   *
   * TODO: Delegate to backend provider via ExtensionPoint.
   */
  async generateResponse(_prompt) {
    throw SDKError.componentNotReady("VoiceAgent", "No WASM backend registered — use a backend package (e.g. @runanywhere/web-llamacpp)");
  }
  /** Get the native handle (used by backend providers). */
  get handle() {
    return this._handle;
  }
  /**
   * Destroy the voice agent session.
   *
   * TODO: Delegate cleanup to backend provider via ExtensionPoint.
   */
  destroy() {
    this._handle = 0;
  }
};
var VoiceAgent = {
  /**
   * Create a standalone VoiceAgent session.
   * The agent manages its own STT, LLM, TTS, and VAD components.
   *
   * TODO: Delegate to backend provider via ExtensionPoint.
   */
  async create() {
    if (!RunAnywhere.isInitialized) {
      throw SDKError.notInitialized();
    }
    throw SDKError.componentNotReady("VoiceAgent", "No WASM backend registered — use a backend package (e.g. @runanywhere/web-llamacpp)");
  }
};

// node_modules/@runanywhere/web/dist/Public/Extensions/RunAnywhere+VoicePipeline.js
var logger10 = new SDKLogger("VoicePipeline");
function requireSTT() {
  return ExtensionPoint.requireProvider("stt", "@runanywhere/web-onnx");
}
function requireTextGeneration() {
  return ExtensionPoint.requireProvider("llm", "@runanywhere/web-llamacpp");
}
function requireTTS() {
  return ExtensionPoint.requireProvider("tts", "@runanywhere/web-onnx");
}
var DEFAULT_OPTIONS = {
  maxTokens: 60,
  temperature: 0.7,
  systemPrompt: "You are a helpful voice assistant. Keep responses concise — 1-2 sentences max.",
  ttsSpeed: 1,
  sampleRate: 16e3
};
var VoicePipeline = class {
  constructor() {
    __publicField(this, "_cancelGeneration", null);
    __publicField(this, "_state", PipelineState.Idle);
  }
  get state() {
    return this._state;
  }
  async processTurn(audioData, options, callbacks) {
    var _a, _b, _c, _d;
    const stt = requireSTT();
    const textGen = requireTextGeneration();
    const tts = requireTTS();
    const opts = { ...DEFAULT_OPTIONS, ...options };
    const totalStart = performance.now();
    this.transition(PipelineState.ProcessingSTT, callbacks);
    const sttStart = performance.now();
    logger10.info(`STT: ${(audioData.length / opts.sampleRate).toFixed(1)}s of audio`);
    const sttResult = await stt.transcribe(audioData, {
      sampleRate: opts.sampleRate
    });
    const sttMs = performance.now() - sttStart;
    const userText = sttResult.text.trim();
    logger10.info(`STT complete: "${userText}" (${sttMs.toFixed(0)}ms)`);
    (_a = callbacks == null ? void 0 : callbacks.onTranscription) == null ? void 0 : _a.call(callbacks, userText, sttResult);
    if (!userText) {
      this.transition(PipelineState.Idle, callbacks);
      return {
        transcription: "",
        response: "",
        timing: { sttMs, llmMs: 0, ttsMs: 0, totalMs: performance.now() - totalStart }
      };
    }
    this.transition(PipelineState.GeneratingResponse, callbacks);
    const llmStart = performance.now();
    const { stream, result: llmResultPromise, cancel } = await textGen.generateStream(userText, {
      maxTokens: opts.maxTokens,
      temperature: opts.temperature,
      systemPrompt: opts.systemPrompt
    });
    this._cancelGeneration = cancel;
    let accumulated = "";
    for await (const token of stream) {
      accumulated += token;
      (_b = callbacks == null ? void 0 : callbacks.onResponseToken) == null ? void 0 : _b.call(callbacks, token, accumulated);
    }
    this._cancelGeneration = null;
    const llmResult = await llmResultPromise;
    const fullResponse = llmResult.text || accumulated;
    const llmMs = performance.now() - llmStart;
    logger10.info(`LLM complete: ${llmResult.tokensUsed} tokens, ${llmResult.tokensPerSecond.toFixed(1)} tok/s (${llmMs.toFixed(0)}ms)`);
    (_c = callbacks == null ? void 0 : callbacks.onResponseComplete) == null ? void 0 : _c.call(callbacks, fullResponse, llmResult);
    if (!fullResponse.trim()) {
      this.transition(PipelineState.Idle, callbacks);
      return {
        transcription: userText,
        response: "",
        timing: { sttMs, llmMs, ttsMs: 0, totalMs: performance.now() - totalStart },
        llmResult
      };
    }
    this.transition(PipelineState.PlayingTTS, callbacks);
    const ttsStart = performance.now();
    const ttsResult = await tts.synthesize(fullResponse.trim(), {
      speed: opts.ttsSpeed
    });
    const ttsMs = performance.now() - ttsStart;
    logger10.info(`TTS complete: ${ttsResult.durationMs}ms audio in ${ttsResult.processingTimeMs}ms`);
    (_d = callbacks == null ? void 0 : callbacks.onSynthesisComplete) == null ? void 0 : _d.call(callbacks, ttsResult.audioData, ttsResult.sampleRate, ttsResult);
    this.transition(PipelineState.Idle, callbacks);
    return {
      transcription: userText,
      response: fullResponse,
      synthesizedAudio: ttsResult.audioData,
      sampleRate: ttsResult.sampleRate,
      timing: {
        sttMs,
        llmMs,
        ttsMs,
        totalMs: performance.now() - totalStart
      },
      llmResult
    };
  }
  cancel() {
    if (this._cancelGeneration) {
      this._cancelGeneration();
      this._cancelGeneration = null;
      logger10.info("Generation cancelled");
    }
  }
  transition(newState, callbacks) {
    var _a;
    this._state = newState;
    (_a = callbacks == null ? void 0 : callbacks.onStateChange) == null ? void 0 : _a.call(callbacks, newState);
  }
};

// node_modules/@runanywhere/web/dist/types/VLMTypes.js
var VLMImageFormat;
(function(VLMImageFormat2) {
  VLMImageFormat2[VLMImageFormat2["FilePath"] = 0] = "FilePath";
  VLMImageFormat2[VLMImageFormat2["RGBPixels"] = 1] = "RGBPixels";
  VLMImageFormat2[VLMImageFormat2["Base64"] = 2] = "Base64";
})(VLMImageFormat || (VLMImageFormat = {}));

// node_modules/@runanywhere/web/dist/types/VADTypes.js
var SpeechActivity;
(function(SpeechActivity2) {
  SpeechActivity2["Started"] = "started";
  SpeechActivity2["Ended"] = "ended";
  SpeechActivity2["Ongoing"] = "ongoing";
})(SpeechActivity || (SpeechActivity = {}));

// node_modules/@runanywhere/web/dist/types.js
var ModelArtifactType;
(function(ModelArtifactType2) {
  ModelArtifactType2["SingleFile"] = "single_file";
  ModelArtifactType2["TarGzArchive"] = "tar_gz_archive";
  ModelArtifactType2["Directory"] = "directory";
})(ModelArtifactType || (ModelArtifactType = {}));

// node_modules/@runanywhere/web/dist/Infrastructure/AudioCapture.js
var logger11 = new SDKLogger("AudioCapture");
var AudioCapture = class {
  constructor(config = {}) {
    __publicField(this, "audioContext", null);
    __publicField(this, "mediaStream", null);
    __publicField(this, "sourceNode", null);
    __publicField(this, "processorNode", null);
    __publicField(this, "_analyser", null);
    __publicField(this, "_animFrameId", null);
    __publicField(this, "_isCapturing", false);
    __publicField(this, "_currentLevel", 0);
    __publicField(this, "_pcmChunks", []);
    __publicField(this, "config");
    __publicField(this, "chunkCallback", null);
    __publicField(this, "levelCallback", null);
    this.config = {
      sampleRate: config.sampleRate ?? 16e3,
      chunkSize: config.chunkSize ?? 1600,
      channels: config.channels ?? 1
    };
  }
  get isCapturing() {
    return this._isCapturing;
  }
  /** Current normalized audio level (0..1), updated per animation frame. */
  get currentLevel() {
    return this._currentLevel;
  }
  /**
   * Get the actual sample rate of the audio context.
   * May differ from requested rate if browser doesn't support it.
   */
  get actualSampleRate() {
    var _a;
    return ((_a = this.audioContext) == null ? void 0 : _a.sampleRate) ?? this.config.sampleRate;
  }
  /** Duration of collected audio in seconds based on configured sample rate. */
  get bufferDurationSeconds() {
    const samples = this._pcmChunks.reduce((acc, c) => acc + c.length, 0);
    return samples / this.config.sampleRate;
  }
  /**
   * Start capturing microphone audio.
   *
   * @param onChunk - Optional callback receiving Float32Array chunks of PCM audio (streaming)
   * @param onLevel - Optional callback invoked per animation frame with audio level 0..1
   * @throws If microphone permission is denied
   */
  async start(onChunk, onLevel) {
    if (this._isCapturing) {
      logger11.debug("Already capturing");
      return;
    }
    this.chunkCallback = onChunk ?? null;
    this.levelCallback = onLevel ?? null;
    this._pcmChunks = [];
    this._currentLevel = 0;
    logger11.info(`Starting audio capture (${this.config.sampleRate}Hz, chunk=${this.config.chunkSize})`);
    try {
      this.mediaStream = await navigator.mediaDevices.getUserMedia({
        audio: {
          sampleRate: { ideal: this.config.sampleRate },
          channelCount: { exact: this.config.channels },
          echoCancellation: true,
          noiseSuppression: true,
          autoGainControl: true
        }
      });
      this.audioContext = new AudioContext({
        sampleRate: this.config.sampleRate
      });
      this.sourceNode = this.audioContext.createMediaStreamSource(this.mediaStream);
      this._analyser = this.audioContext.createAnalyser();
      this._analyser.fftSize = 256;
      this.sourceNode.connect(this._analyser);
      const bufferSize = this.nearestPowerOf2(this.config.chunkSize);
      this.processorNode = this.audioContext.createScriptProcessor(bufferSize, this.config.channels, this.config.channels);
      this.processorNode.onaudioprocess = (event) => {
        var _a;
        if (!this._isCapturing)
          return;
        const inputData = event.inputBuffer.getChannelData(0);
        const samples = new Float32Array(inputData.length);
        samples.set(inputData);
        this._pcmChunks.push(samples);
        (_a = this.chunkCallback) == null ? void 0 : _a.call(this, samples);
      };
      this.sourceNode.connect(this.processorNode);
      this.processorNode.connect(this.audioContext.destination);
      this._isCapturing = true;
      this.startLevelMonitoring();
      logger11.info("Audio capture started");
    } catch (error) {
      this.cleanupResources();
      const message = error instanceof Error ? error.message : String(error);
      logger11.error(`Failed to start audio capture: ${message}`);
      throw new Error(`Microphone access failed: ${message}`);
    }
  }
  /**
   * Stop capturing audio and release resources.
   */
  stop() {
    if (!this._isCapturing)
      return;
    this._isCapturing = false;
    this._currentLevel = 0;
    this.chunkCallback = null;
    this.levelCallback = null;
    this.cleanupResources();
    logger11.info("Audio capture stopped");
  }
  /**
   * Get all collected PCM audio as a single Float32Array.
   * Does NOT clear the buffer — call `clearBuffer()` separately.
   */
  getAudioBuffer() {
    if (this._pcmChunks.length === 0)
      return new Float32Array(0);
    const totalLength = this._pcmChunks.reduce((acc, c) => acc + c.length, 0);
    const merged = new Float32Array(totalLength);
    let offset = 0;
    for (const chunk of this._pcmChunks) {
      merged.set(chunk, offset);
      offset += chunk.length;
    }
    return merged;
  }
  /**
   * Drain: return the current buffer and clear it for the next segment.
   * Useful for live mode where we transcribe segments incrementally.
   */
  drainBuffer() {
    const buffer = this.getAudioBuffer();
    this._pcmChunks = [];
    return buffer;
  }
  /** Clear collected PCM data without stopping capture. */
  clearBuffer() {
    this._pcmChunks = [];
  }
  /** Start the requestAnimationFrame loop that reads the AnalyserNode. */
  startLevelMonitoring() {
    const analyser = this._analyser;
    if (!analyser)
      return;
    const dataArray = new Uint8Array(analyser.frequencyBinCount);
    const tick = () => {
      var _a;
      if (!this._isCapturing || !this._analyser)
        return;
      this._analyser.getByteFrequencyData(dataArray);
      let sum = 0;
      for (let i = 0; i < dataArray.length; i++)
        sum += dataArray[i];
      const avg = sum / dataArray.length / 255;
      this._currentLevel = avg;
      (_a = this.levelCallback) == null ? void 0 : _a.call(this, avg);
      this._animFrameId = requestAnimationFrame(tick);
    };
    this._animFrameId = requestAnimationFrame(tick);
  }
  cleanupResources() {
    if (this._animFrameId !== null) {
      cancelAnimationFrame(this._animFrameId);
      this._animFrameId = null;
    }
    if (this.processorNode) {
      this.processorNode.disconnect();
      this.processorNode.onaudioprocess = null;
      this.processorNode = null;
    }
    if (this._analyser) {
      this._analyser.disconnect();
      this._analyser = null;
    }
    if (this.sourceNode) {
      this.sourceNode.disconnect();
      this.sourceNode = null;
    }
    if (this.audioContext) {
      this.audioContext.close().catch(() => {
      });
      this.audioContext = null;
    }
    if (this.mediaStream) {
      this.mediaStream.getTracks().forEach((track) => track.stop());
      this.mediaStream = null;
    }
  }
  nearestPowerOf2(n) {
    let power = 256;
    while (power < n && power < 16384) {
      power *= 2;
    }
    return power;
  }
};

// node_modules/@runanywhere/web/dist/Infrastructure/AudioPlayback.js
var logger12 = new SDKLogger("AudioPlayback");
var AudioPlayback = class {
  constructor(config = {}) {
    __publicField(this, "audioContext", null);
    __publicField(this, "gainNode", null);
    __publicField(this, "currentSource", null);
    __publicField(this, "_isPlaying", false);
    __publicField(this, "config");
    this.config = {
      sampleRate: config.sampleRate ?? 22050,
      volume: config.volume ?? 1
    };
  }
  get isPlaying() {
    return this._isPlaying;
  }
  /**
   * Play a Float32Array of PCM audio samples.
   *
   * @param samples - PCM audio data (Float32Array)
   * @param sampleRate - Sample rate (overrides config)
   * @returns Promise that resolves when playback completes
   */
  async play(samples, sampleRate) {
    this.stop();
    const rate = sampleRate ?? this.config.sampleRate;
    const durationMs = samples.length / rate * 1e3;
    logger12.debug(`Playing ${samples.length} samples at ${rate}Hz (${(durationMs / 1e3).toFixed(1)}s)`);
    if (!this.audioContext || this.audioContext.state === "closed") {
      this.audioContext = new AudioContext({ sampleRate: rate });
    }
    if (this.audioContext.state === "suspended") {
      await this.audioContext.resume();
    }
    if (!this.gainNode || this.gainNode.context !== this.audioContext) {
      this.gainNode = this.audioContext.createGain();
      this.gainNode.connect(this.audioContext.destination);
    }
    this.gainNode.gain.value = this.config.volume;
    const audioBuffer = this.audioContext.createBuffer(1, samples.length, rate);
    audioBuffer.getChannelData(0).set(samples);
    return new Promise((resolve) => {
      const source = this.audioContext.createBufferSource();
      source.buffer = audioBuffer;
      source.connect(this.gainNode);
      source.onended = () => {
        this._isPlaying = false;
        this.currentSource = null;
        EventBus.shared.emit("playback.completed", SDKEventType.Voice, { durationMs });
        resolve();
      };
      this.currentSource = source;
      this._isPlaying = true;
      EventBus.shared.emit("playback.started", SDKEventType.Voice, { durationMs, sampleRate: rate });
      source.start();
    });
  }
  /**
   * Stop playback immediately.
   */
  stop() {
    if (this.currentSource) {
      try {
        this.currentSource.stop();
      } catch {
      }
      this.currentSource = null;
    }
    this._isPlaying = false;
  }
  /**
   * Set playback volume.
   */
  setVolume(volume) {
    this.config.volume = Math.max(0, Math.min(1, volume));
    if (this.gainNode) {
      this.gainNode.gain.value = this.config.volume;
    }
  }
  /**
   * Release all audio resources.
   */
  dispose() {
    this.stop();
    if (this.audioContext && this.audioContext.state !== "closed") {
      this.audioContext.close().catch(() => {
      });
    }
    this.audioContext = null;
    this.gainNode = null;
  }
};

// node_modules/@runanywhere/web/dist/Infrastructure/AudioFileLoader.js
var AudioFileLoader = class {
  /**
   * Decode an audio File to a mono Float32Array PCM buffer.
   * Resamples to targetSampleRate if the file's native rate differs.
   *
   * @param file            Any browser-supported audio file
   * @param targetSampleRate Output sample rate (default: 16000 Hz for STT models)
   */
  static async toFloat32Array(file, targetSampleRate = 16e3) {
    const arrayBuffer = await file.arrayBuffer();
    const nativeCtx = new AudioContext();
    let audioBuffer;
    try {
      audioBuffer = await nativeCtx.decodeAudioData(arrayBuffer);
    } finally {
      await nativeCtx.close();
    }
    const durationSeconds = audioBuffer.duration;
    if (audioBuffer.sampleRate === targetSampleRate) {
      return {
        samples: new Float32Array(audioBuffer.getChannelData(0)),
        sampleRate: targetSampleRate,
        durationSeconds
      };
    }
    const targetLength = Math.ceil(durationSeconds * targetSampleRate);
    const offlineCtx = new OfflineAudioContext(1, targetLength, targetSampleRate);
    const source = offlineCtx.createBufferSource();
    source.buffer = audioBuffer;
    source.connect(offlineCtx.destination);
    source.start();
    const resampled = await offlineCtx.startRendering();
    return {
      samples: new Float32Array(resampled.getChannelData(0)),
      sampleRate: targetSampleRate,
      durationSeconds
    };
  }
};

// node_modules/@runanywhere/web/dist/Infrastructure/VideoCapture.js
var logger13 = new SDKLogger("VideoCapture");
var VideoCapture = class _VideoCapture {
  constructor(config = {}) {
    __publicField(this, "config");
    __publicField(this, "_mediaStream", null);
    __publicField(this, "_videoEl");
    __publicField(this, "_canvasEl");
    __publicField(this, "_isCapturing", false);
    __publicField(this, "_startPromise", null);
    this.config = {
      facingMode: config.facingMode ?? "environment",
      idealWidth: config.idealWidth ?? 640,
      idealHeight: config.idealHeight ?? 480
    };
    this._videoEl = document.createElement("video");
    this._videoEl.playsInline = true;
    this._videoEl.autoplay = true;
    this._videoEl.muted = true;
    this._canvasEl = document.createElement("canvas");
  }
  // ---- Public getters ----
  /** Whether the camera is currently capturing. */
  get isCapturing() {
    return this._isCapturing;
  }
  /**
   * The HTMLVideoElement receiving the camera stream.
   * Attach this to the DOM for live preview:
   *
   * ```typescript
   * previewContainer.appendChild(camera.videoElement);
   * ```
   */
  get videoElement() {
    return this._videoEl;
  }
  /** Native video width from the camera (0 if not started). */
  get videoWidth() {
    return this._videoEl.videoWidth;
  }
  /** Native video height from the camera (0 if not started). */
  get videoHeight() {
    return this._videoEl.videoHeight;
  }
  // ---- Lifecycle ----
  /**
   * Start the camera and begin capturing video.
   *
   * Requests camera permission via `getUserMedia`. The returned Promise
   * resolves once the video stream is active and ready for frame capture.
   *
   * @throws If camera permission is denied or no camera is available.
   */
  async start() {
    if (this._isCapturing) {
      logger13.debug("Already capturing");
      return;
    }
    if (this._startPromise) {
      logger13.debug("Start already in progress, awaiting existing attempt");
      return this._startPromise;
    }
    this._startPromise = this._doStart();
    try {
      await this._startPromise;
    } finally {
      this._startPromise = null;
    }
  }
  async _doStart() {
    logger13.info(`Starting video capture (${this.config.facingMode}, ${this.config.idealWidth}x${this.config.idealHeight})`);
    try {
      this._mediaStream = await navigator.mediaDevices.getUserMedia({
        video: {
          facingMode: this.config.facingMode,
          width: { ideal: this.config.idealWidth },
          height: { ideal: this.config.idealHeight }
        },
        audio: false
      });
      this._videoEl.srcObject = this._mediaStream;
      await new Promise((resolve, reject) => {
        const timeout = setTimeout(() => reject(new Error("Video stream timeout")), 1e4);
        this._videoEl.onloadedmetadata = () => {
          clearTimeout(timeout);
          resolve();
        };
      });
      this._isCapturing = true;
      logger13.info(`Camera started (${this._videoEl.videoWidth}x${this._videoEl.videoHeight})`);
    } catch (error) {
      this.cleanupResources();
      const message = error instanceof Error ? error.message : String(error);
      logger13.error(`Failed to start video capture: ${message}`);
      throw new Error(`Camera access failed: ${message}`);
    }
  }
  /**
   * Stop capturing video and release camera resources.
   */
  stop() {
    if (!this._isCapturing)
      return;
    this._isCapturing = false;
    this.cleanupResources();
    logger13.info("Video capture stopped");
  }
  // ---- Frame Capture ----
  /**
   * Capture the current video frame as raw RGB pixels.
   *
   * The frame is downscaled to fit within `maxDimension` while preserving
   * aspect ratio. The CLIP encoder resizes to its fixed input size
   * internally, so capturing at larger sizes only wastes WASM copy time.
   *
   * @param maxDimension  Maximum width or height in pixels (default: 512).
   * @returns CapturedFrame or null if the video stream isn't ready.
   */
  captureFrame(maxDimension = 512) {
    if (!this._isCapturing || !this._videoEl.videoWidth || !this._videoEl.videoHeight) {
      return null;
    }
    const { w, h } = _VideoCapture.fitSize(this._videoEl.videoWidth, this._videoEl.videoHeight, maxDimension);
    this._canvasEl.width = w;
    this._canvasEl.height = h;
    const ctx = this._canvasEl.getContext("2d");
    if (!ctx)
      return null;
    ctx.drawImage(this._videoEl, 0, 0, w, h);
    return _VideoCapture.extractRGB(ctx, w, h);
  }
  // ---- Static Utilities ----
  /**
   * Compute a downscaled size that fits within `maxDim` while
   * preserving aspect ratio. Returns original size if already small enough.
   */
  static fitSize(srcW, srcH, maxDim) {
    if (srcW <= maxDim && srcH <= maxDim) {
      return { w: srcW, h: srcH };
    }
    const scale = maxDim / Math.max(srcW, srcH);
    return {
      w: Math.round(srcW * scale),
      h: Math.round(srcH * scale)
    };
  }
  /**
   * Extract raw RGB pixels from a canvas 2D context.
   *
   * Canvas gives RGBA; this strips the alpha channel to produce the
   * RGBRGBRGB... byte layout expected by the C++ VLM backend
   * (RAC_VLM_IMAGE_FORMAT_RGB_PIXELS).
   */
  static extractRGB(ctx, w, h) {
    const imageData = ctx.getImageData(0, 0, w, h);
    const rgba = imageData.data;
    const pixelCount = w * h;
    const rgb = new Uint8Array(pixelCount * 3);
    for (let i = 0; i < pixelCount; i++) {
      const src = i * 4;
      const dst = i * 3;
      rgb[dst] = rgba[src];
      rgb[dst + 1] = rgba[src + 1];
      rgb[dst + 2] = rgba[src + 2];
    }
    return { rgbPixels: rgb, width: w, height: h };
  }
  // ---- Internals ----
  cleanupResources() {
    if (this._mediaStream) {
      this._mediaStream.getTracks().forEach((track) => track.stop());
      this._mediaStream = null;
    }
    this._videoEl.srcObject = null;
  }
};

// node_modules/@runanywhere/web/dist/Infrastructure/DeviceCapabilities.js
var logger14 = new SDKLogger("DeviceCapabilities");
async function detectCapabilities() {
  const capabilities = {
    hasWebGPU: false,
    activeAcceleration: "cpu",
    hasSharedArrayBuffer: typeof SharedArrayBuffer !== "undefined",
    isCrossOriginIsolated: typeof crossOriginIsolated !== "undefined" ? crossOriginIsolated : false,
    hasWASMSIMD: detectWASMSIMD(),
    hasOPFS: typeof navigator !== "undefined" && "storage" in navigator && "getDirectory" in navigator.storage,
    deviceMemoryGB: navigator.deviceMemory ?? 4,
    hardwareConcurrency: navigator.hardwareConcurrency ?? 4,
    userAgent: navigator.userAgent
  };
  if (typeof navigator !== "undefined" && "gpu" in navigator) {
    try {
      const gpu = navigator.gpu;
      const adapter = await (gpu == null ? void 0 : gpu.requestAdapter());
      if (adapter) {
        capabilities.hasWebGPU = true;
        try {
          const info = await adapter.requestAdapterInfo();
          capabilities.gpuAdapterInfo = {
            vendor: info.vendor ?? "",
            architecture: info.architecture ?? "",
            description: info.description ?? ""
          };
        } catch {
        }
      }
    } catch {
      logger14.debug("WebGPU detection failed");
    }
  }
  logger14.info(`Capabilities: WebGPU=${capabilities.hasWebGPU}, SharedArrayBuffer=${capabilities.hasSharedArrayBuffer}, SIMD=${capabilities.hasWASMSIMD}, OPFS=${capabilities.hasOPFS}, Memory=${capabilities.deviceMemoryGB}GB, Cores=${capabilities.hardwareConcurrency}`);
  if (!capabilities.isCrossOriginIsolated) {
    logger14.warning("Cross-Origin Isolation is NOT enabled. SharedArrayBuffer and multi-threaded WASM will be unavailable. Set these HTTP headers on your server:\n  Cross-Origin-Opener-Policy: same-origin\n  Cross-Origin-Embedder-Policy: credentialless\nSee: https://github.com/AnywhereAI/runanywhere-sdks/tree/main/sdk/runanywhere-web#cross-origin-isolation-headers");
  }
  return capabilities;
}
async function getDeviceInfo() {
  const caps = await detectCapabilities();
  return {
    model: "Browser",
    name: getBrowserName(caps.userAgent),
    osVersion: getOSVersion(caps.userAgent),
    totalMemory: caps.deviceMemoryGB * 1024 * 1024 * 1024,
    architecture: "wasm32",
    hasWebGPU: caps.hasWebGPU,
    hasSharedArrayBuffer: caps.hasSharedArrayBuffer
  };
}
function detectWASMSIMD() {
  try {
    const simdTest = new Uint8Array([
      0,
      97,
      115,
      109,
      1,
      0,
      0,
      0,
      1,
      5,
      1,
      96,
      0,
      1,
      123,
      3,
      2,
      1,
      0,
      10,
      10,
      1,
      8,
      0,
      65,
      0,
      253,
      15,
      253,
      98,
      11
    ]);
    return WebAssembly.validate(simdTest);
  } catch {
    return false;
  }
}
function getBrowserName(ua) {
  if (ua.includes("Firefox"))
    return "Firefox";
  if (ua.includes("Edg/"))
    return "Edge";
  if (ua.includes("Chrome"))
    return "Chrome";
  if (ua.includes("Safari"))
    return "Safari";
  return "Unknown Browser";
}
function getOSVersion(ua) {
  if (ua.includes("Windows"))
    return "Windows";
  if (ua.includes("Mac OS X"))
    return "macOS";
  if (ua.includes("Linux"))
    return "Linux";
  if (ua.includes("Android"))
    return "Android";
  if (ua.includes("iOS"))
    return "iOS";
  return "Unknown OS";
}

// node_modules/@runanywhere/web/dist/Infrastructure/ArchiveUtility.js
var TAR_BLOCK_SIZE = 512;
var TarFileType;
(function(TarFileType2) {
  TarFileType2[TarFileType2["Regular"] = 48] = "Regular";
  TarFileType2[TarFileType2["RegularAlt"] = 0] = "RegularAlt";
  TarFileType2[TarFileType2["Directory"] = 53] = "Directory";
})(TarFileType || (TarFileType = {}));
async function extractTarGz(tarGzData) {
  const tarData = await decompressGzip(tarGzData);
  return parseTar(tarData);
}
async function decompressGzip(compressed) {
  const ds = new DecompressionStream("gzip");
  const writer = ds.writable.getWriter();
  const reader = ds.readable.getReader();
  writer.write(compressed);
  writer.close();
  const chunks = [];
  let totalLength = 0;
  while (true) {
    const { done, value } = await reader.read();
    if (done)
      break;
    chunks.push(value);
    totalLength += value.length;
  }
  const result = new Uint8Array(totalLength);
  let offset = 0;
  for (const chunk of chunks) {
    result.set(chunk, offset);
    offset += chunk.length;
  }
  return result;
}
function parseTar(tarData) {
  const entries = [];
  let offset = 0;
  while (offset + TAR_BLOCK_SIZE <= tarData.length) {
    const header = tarData.subarray(offset, offset + TAR_BLOCK_SIZE);
    if (isZeroBlock(header))
      break;
    const name = readString(header, 0, 100);
    const size = readOctal(header, 124, 12);
    const typeFlag = header[156];
    const prefix = readString(header, 345, 155);
    const fullPath = prefix ? `${prefix}/${name}` : name;
    const normalisedPath = fullPath.replace(/^\.\//, "").replace(/^\//, "");
    offset += TAR_BLOCK_SIZE;
    if (size > 0) {
      const dataStart = offset;
      const dataEnd = dataStart + size;
      if (typeFlag === TarFileType.Regular || typeFlag === TarFileType.RegularAlt) {
        entries.push({
          path: normalisedPath,
          data: tarData.slice(dataStart, dataEnd)
        });
      }
      offset += Math.ceil(size / TAR_BLOCK_SIZE) * TAR_BLOCK_SIZE;
    }
  }
  return entries;
}
function readString(header, offset, length) {
  let end = offset;
  const limit = offset + length;
  while (end < limit && header[end] !== 0)
    end++;
  const decoder = new TextDecoder("ascii");
  return decoder.decode(header.subarray(offset, end));
}
function readOctal(header, offset, length) {
  const str = readString(header, offset, length).trim();
  return str ? parseInt(str, 8) : 0;
}
function isZeroBlock(block) {
  for (let i = 0; i < block.length; i++) {
    if (block[i] !== 0)
      return false;
  }
  return true;
}

// node_modules/@runanywhere/web/dist/services/HTTPService.js
var logger15 = new SDKLogger("HTTPService");
var SDK_CLIENT = "RunAnywhereSDK";
var SDK_PLATFORM = "web";
var SDK_VERSION = "0.1.0-beta.8";
var DEFAULT_TIMEOUT_MS = 3e4;
var DEVICE_ID_KEY = "rac_device_id";
var TELEMETRY_TABLE = "rest/v1/telemetry_events";
var _HTTPService = class _HTTPService {
  constructor() {
    __publicField(this, "baseURL", "");
    __publicField(this, "apiKey", "");
    __publicField(this, "environment", SDKEnvironment.Production);
    __publicField(this, "accessToken", null);
    __publicField(this, "timeoutMs", DEFAULT_TIMEOUT_MS);
    __publicField(this, "supabaseURL", "");
    __publicField(this, "supabaseKey", "");
  }
  static get shared() {
    if (!_HTTPService._instance) {
      _HTTPService._instance = new _HTTPService();
    }
    return _HTTPService._instance;
  }
  get defaultHeaders() {
    return {
      "Content-Type": "application/json",
      "Accept": "application/json",
      "X-SDK-Client": SDK_CLIENT,
      "X-SDK-Version": SDK_VERSION,
      "X-Platform": SDK_PLATFORM
    };
  }
  // ---------------------------------------------------------------------------
  // Telemetry helpers
  // ---------------------------------------------------------------------------
  /**
   * @deprecated Use `AnalyticsEmitter` instead.  All telemetry now routes
   * through the C++ telemetry manager via `rac_analytics_emit_*()`.
   * Kept as a fallback for edge cases where C++ is unavailable.
   */
  postTelemetryEvent(partialPayload) {
    if (!this.isConfigured)
      return;
    const payload = {
      sdk_event_id: crypto.randomUUID(),
      event_timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      created_at: (/* @__PURE__ */ new Date()).toISOString(),
      device_id: this.getOrCreateDeviceId(),
      platform: SDK_PLATFORM,
      sdk_version: SDK_VERSION,
      ...partialPayload
    };
    const url = this.buildFullURL(TELEMETRY_TABLE);
    const headers = this.buildHeaders(false);
    this.executeRequest("POST", url, headers, [payload]).catch(() => {
    });
  }
  /**
   * Returns the persistent device UUID, creating one if it doesn't exist.
   * Mirrors getOrCreateDeviceId() in TelemetryService.ts.
   */
  getOrCreateDeviceId() {
    try {
      const existing = localStorage.getItem(DEVICE_ID_KEY);
      if (existing)
        return existing;
      const id = crypto.randomUUID();
      localStorage.setItem(DEVICE_ID_KEY, id);
      return id;
    } catch {
      return crypto.randomUUID();
    }
  }
  // ---------------------------------------------------------------------------
  // Configuration
  // ---------------------------------------------------------------------------
  configure(config) {
    this.baseURL = config.baseURL;
    this.apiKey = config.apiKey;
    this.environment = config.environment;
    this.timeoutMs = config.timeoutMs ?? DEFAULT_TIMEOUT_MS;
    logger15.info(`Configured for ${config.environment} environment`);
  }
  /**
   * Configure development mode using Supabase credentials.
   * Called during WASM init using credentials read from rac_dev_config_*.
   */
  configureDev(config) {
    this.supabaseURL = config.supabaseURL;
    this.supabaseKey = config.supabaseKey;
    this.environment = SDKEnvironment.Development;
    logger15.info("Development mode configured with Supabase");
  }
  setToken(token) {
    this.accessToken = token;
  }
  clearToken() {
    this.accessToken = null;
  }
  get isConfigured() {
    if (this.environment === SDKEnvironment.Development) {
      return !!this.supabaseURL;
    }
    return !!this.baseURL && !!this.apiKey;
  }
  get currentBaseURL() {
    if (this.environment === SDKEnvironment.Development && this.supabaseURL) {
      return this.supabaseURL;
    }
    return this.baseURL;
  }
  // ---------------------------------------------------------------------------
  // HTTP Methods
  // ---------------------------------------------------------------------------
  async post(path, data) {
    let url = this.buildFullURL(path);
    const isDeviceReg = this.isDeviceRegistrationPath(path);
    const headers = this.buildHeaders(isDeviceReg);
    if (isDeviceReg && this.environment === SDKEnvironment.Development) {
      const separator = url.includes("?") ? "&" : "?";
      url = `${url}${separator}on_conflict=device_id`;
    }
    const response = await this.executeRequest("POST", url, headers, data);
    if (isDeviceReg && response.status === 409) {
      logger15.info("Device already registered (409) — treating as success");
      return this.parseResponse(response);
    }
    return this.handleResponse(response, path);
  }
  async get(path) {
    const url = this.buildFullURL(path);
    const headers = this.buildHeaders(false);
    const response = await this.executeRequest("GET", url, headers);
    return this.handleResponse(response, path);
  }
  async delete(path) {
    const url = this.buildFullURL(path);
    const headers = this.buildHeaders(false);
    const response = await this.executeRequest("DELETE", url, headers);
    return this.handleResponse(response, path);
  }
  // ---------------------------------------------------------------------------
  // Private
  // ---------------------------------------------------------------------------
  async executeRequest(method, url, headers, data) {
    logger15.debug(`${method} ${url}`);
    const controller = new AbortController();
    const timeoutId = setTimeout(() => controller.abort(), this.timeoutMs);
    try {
      const options = { method, headers, signal: controller.signal };
      if (data !== void 0 && method !== "GET") {
        options.body = JSON.stringify(data);
      }
      return await fetch(url, options);
    } finally {
      clearTimeout(timeoutId);
    }
  }
  buildHeaders(isDeviceRegistration) {
    const headers = { ...this.defaultHeaders };
    if (this.environment === SDKEnvironment.Development) {
      if (this.supabaseKey) {
        headers["apikey"] = this.supabaseKey;
        headers["Authorization"] = `Bearer ${this.supabaseKey}`;
        headers["Prefer"] = isDeviceRegistration ? "resolution=merge-duplicates" : "return=representation";
      }
    } else {
      const token = this.accessToken || this.apiKey;
      if (token) {
        headers["Authorization"] = `Bearer ${token}`;
      }
    }
    return headers;
  }
  buildFullURL(path) {
    if (path.startsWith("http://") || path.startsWith("https://")) {
      return path;
    }
    const base = this.currentBaseURL.replace(/\/$/, "");
    const endpoint = path.startsWith("/") ? path : `/${path}`;
    return `${base}${endpoint}`;
  }
  isDeviceRegistrationPath(path) {
    return path.includes("sdk_devices") || path.includes("devices/register") || path.includes("rest/v1/sdk_devices");
  }
  async parseResponse(response) {
    const text = await response.text();
    if (!text)
      return {};
    try {
      return JSON.parse(text);
    } catch {
      return text;
    }
  }
  async handleResponse(response, path) {
    if (response.ok) {
      return this.parseResponse(response);
    }
    let errorMessage = `HTTP ${response.status}`;
    try {
      const errorData = await response.json();
      errorMessage = errorData.message || errorData.error || errorMessage;
    } catch {
    }
    if (path.includes("telemetry")) {
      logger15.debug(`HTTP ${response.status}: ${path}`);
    } else {
      logger15.error(`HTTP ${response.status}: ${path}`);
    }
    throw this.mapHttpError(response.status, errorMessage);
  }
  mapHttpError(status, message) {
    switch (status) {
      case 401:
      case 403:
        return new SDKError(SDKErrorCode.AuthenticationFailed, message);
      case 408:
      case 429:
        return new SDKError(SDKErrorCode.NetworkTimeout, message);
      default:
        return new SDKError(SDKErrorCode.NetworkError, `HTTP ${status}: ${message}`);
    }
  }
};
__publicField(_HTTPService, "_instance", null);
var HTTPService = _HTTPService;
export {
  AccelerationPreference,
  AnalyticsEmitter,
  AudioCapture,
  AudioFileLoader,
  AudioPlayback,
  BackendCapability,
  ComponentState,
  ConfigurationSource,
  DownloadStage,
  EventBus,
  ExecutionTarget,
  ExtensionPoint,
  ExtensionRegistry,
  FrameworkModality,
  HTTPService,
  HardwareAcceleration,
  LLMFramework,
  LocalFileStorage,
  LogLevel,
  ModelArtifactType,
  ModelCategory,
  ModelFormat,
  ModelManager,
  ModelStatus,
  OPFSStorage,
  PipelineState,
  RoutingPolicy,
  RunAnywhere,
  SDKComponent,
  SDKEnvironment,
  SDKError,
  SDKErrorCode,
  SDKEventType,
  SDKLogger,
  ServiceKey,
  SpeechActivity,
  VLMImageFormat,
  VideoCapture,
  VoiceAgent,
  VoiceAgentSession,
  VoicePipeline,
  detectCapabilities,
  extractTarGz,
  getDeviceInfo,
  inferModelFromFilename,
  isSDKError,
  sanitizeId
};
//# sourceMappingURL=@runanywhere_web.js.map
